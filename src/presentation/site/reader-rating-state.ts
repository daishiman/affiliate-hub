/**
 * 読者の評価まわりの定数。
 *
 * `"use server"` のファイルからは非同期の関数しか外へ出せない
 * （出すと、その名前がそのまま外から呼べる入口になる）。
 * 目印の置き場所は画面側からも読みたいので、ここに置く。
 */

/** 二度押しを見分けるための、この端末の目印を入れる場所。 */
export const READER_KEY_COOKIE = "ah_reader_key";

/** 1 年。短すぎると同じ人が何度も押せる。長すぎても実害が無いのでこの長さ。 */
export const READER_KEY_MAX_AGE = 60 * 60 * 24 * 365;
