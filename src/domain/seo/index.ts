/**
 * 検索最適化 (改善層の SEO 側)。
 *
 * 記事ごとの検証可能な指摘 (`assessment`)、準拠すべき公式ガイドラインの
 * 出典と鮮度 (`guideline-reference`)、更新の通知 (`indexnow`) をまとめる。
 * 診断は下書きまでを出し、公開面は既存の承認経路が書く。
 */
export * from "./assessment";
export * from "./guideline-reference";
export * from "./indexnow";
