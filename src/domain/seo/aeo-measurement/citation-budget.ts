/**
 * 被引用チェックの実行上限（受入 A4・NFR9）。
 *
 * ==========================================================================
 * なぜ上限が必要か
 * ==========================================================================
 *
 * 系統③（AI 検索での被引用チェック）は、1 ページ調べるごとに外部へ
 * 問い合わせる。**費用がページ数に比例して増える。** 記事が増えるほど
 * 毎週の請求が増え、増えたことは請求書が来るまで分からない。
 *
 * 上限に当たったときに黙って止まると、今度は「調べていないページ」が
 * 「引用されていないページ」と区別できなくなる。だから
 * **止めた事実を必ず記録して返す**（A4）。
 */

/** 1回の収集で調べるページ数。月次のweb検索回数とは別の上限。 */
export const DEFAULT_CITATION_PAGES_PER_RUN = 50;
