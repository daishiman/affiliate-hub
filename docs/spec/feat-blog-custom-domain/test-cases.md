# テストケース — 住所層

実在する `it(...)` の題を写している。追加・削除したときはここも直す。

## `tests/application/manage-custom-domains.test.ts` (22 件)

### 入れられる住所と、入れられない住所
- 大文字も末尾のドットも同じ住所として受け取る … INV-10
- スキームやパスが混ざった入力は、黙って削らずに断る
- ドットの無い名前は受け取らない

### 外部が落ちていても、済んだことは済んだと言う
- 申し込みに失敗しても登録は残り、何が残っているかを伝える
- 外部 id をまだ持たない行の「今すぐ確認」は、申し込みからやり直す
- 同じ状態を何度写し取っても結果は変わらない … A10 (冪等)

### 取り下げは外部の可用性に依存しない
- 外部の取り消しが落ちても、読者への配信は止まっている … INV-12
- こちらを止めてから外部を消す（順序が逆だと外部の故障で止められない）
- 理由の無い取り下げは受け付けない … A7

### 記録が書けなかったら「保存できた」と言わない
- 監査に書けないと失敗として返る … INV-11
- 成功した操作は記録に残る … A8

### 見るのと変えるのは別の権限
- 記事を書く人は住所の一覧を見られる
- 記事を書く人は住所を登録できない … A7
- 記事を書く人は正規の住所を切り替えられない … A7

## `tests/integration/d1-custom-domain.test.ts` (workerd 実機)

### 既定住所と独自ドメインの関係
- 1 件も登録していないブログは行を持たない（既定住所は行ではない） … INV-1 / A4
- 登録直後は pending で、正規の住所にはなっていない … A1

### 取り下げは終端である
- 取り下げた後に外部が active を運んできても復活しない … INV-4 / A6
- 取り下げた住所は、新しい行として登録し直せる（部分ユニーク索引の要） … INV-3
- 同じ住所を生きたまま 2 度登録することはできない … INV-2 / A2

### 正規の住所はブログごとに 1 つ
- 切り替えると前の行の canonical が降りる … INV-5 / A5
- 配信中でない住所は正規にできない … INV-6
- active から落ちると canonical も同時に降りる（画面と配信先を食い違わせない） … INV-7

### 公開側の照会と所有境界
- 配信中の住所だけがブログへ解決する … INV-8 / A1
- 正規の住所は逆向きにも引ける（canonical を組むため） … A5
- 他の workspace の id を指しても触れない … INV-9

## 入口の配線 (`tests/infrastructure/resolve-custom-host.test.ts`)

住所表の**引き方**ではなく、入口が持つ**写しの規則**を固定する。

- ポート番号を落とす（手元の `:3000` が住所として流れないように） … A3
- 大文字を小文字に倒す / ホスト名として成り立たない値は null … A3
- 同じホストの2回目は照会しない … A3
- 見つからなかったことも写す（未使用の環境で毎回引かない） … A3
- 寿命を過ぎたら引き直す（取り下げがここで効く） … A3 / A6
- 照会が投げたときは写さない（一瞬の障害を寿命ぶん固定しない） … A3
- 知らないホスト名を大量に投げられても写しが際限なく太らない … A3
- ホスト名にならない値では照会しない / ポート違いは同じ住所として1回だけ引く … A3
- 逆向き: 同じブログの2回目は照会しない / 寿命を過ぎたら引き直す … A5
- 逆向き: 住所の写しとブログの写しは混ざらない / URL 名が空なら照会しない … A5

## 住所が決まったあとの判断 (`tests/domain/authoring/site-hostname.test.ts`)

- 独自ドメインはここでは決まらない（住所表を引くのは入口の仕事） … A3
- `routeResolvedSite`: 読者のページはそのブログの画面へ差し替える … A3
- `routeResolvedSite`: 管理画面と管理用 API は出さない … A3
- `routeResolvedSite`: `/s/...` を重ねて呼ぶ経路は作らせない … A3
- `routeResolvedSite`: 前方一致だけで巻き込まない … A3
- `isAlwaysPassPath`: 画面の部品は住所を引く前に落とす / 読者のページは落とさない … A3

## 正本の住所 (`tests/domain/authoring/site-public-url.test.ts` / `tests/presentation/site-metadata.test.ts`)

- 生きた独自ドメインがあればそれが正本 … A5
- 独自ドメインが無ければ既定のサブドメインが正本 … A5
- 基底ドメインが未設定の環境だけ path 形が正本 … A5
- どの住所で来ても同じ 1 本を指す … A5
- 住所を組み立てる材料が何も無ければ配らない … A5
- 住所になっていない基底ドメインは使わない（推測した canonical を配らない） … A5
- 表紙は余分な `/` を付けない … A5
- 生きた独自ドメインがあれば、どの住所で来てもそこを正本にする … A5
- 独自ドメインが無ければ既定のサブドメインを正本にする（自分で 404 を指さない） … A5
- 住所表が読めないときは既定の住所へ倒す（canonicalごと落とさない） … A5

## 覆えていないもの

- **A3 の端から端まで (HTTP で)** — 判断 (`routeResolvedSite`)・照会 (`resolveSiteSlugByHost`)・
  写し (`resolveCustomHostSlug`)・配線 (`middleware.ts`) の 4 つはそれぞれ覆えているが、
  実際に HTTP 要求を投げて 200 を確かめる試験は無い。`middleware` の試験には
  Next.js の実行環境ごと起こす仕掛けが要り、この feature の範囲を超える。
  `tests/e2e/` の担当として残す。
- **A9 の画面上の文言** — `lastError` を行に出すことは `blog-domain-form.tsx` にあるが、
  失敗理由が入った行を描く試験は `tests/ui/blog-ops-console-forms.test.tsx` に無い。
