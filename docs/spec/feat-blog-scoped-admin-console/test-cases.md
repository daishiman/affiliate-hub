# テストケース — ブログごとの管理画面 (feat-blog-scoped-admin-console)

P05 の成果物。実在するケース名。

## `tests/ui/app-shell-nav.test.tsx` — normal / 10 件

1. ブランドから運営ホームへ戻れ、本文へ直接移動できる
2. 一覧・作成・編集が同じ運営画面の見出し順を使う
3. 分類ごとに、まとまりの印と、それが指す見出しが出る
4. 見出しは、押せるものではなく見出しとして出る
5. 何も見えない人には、分類の見出しも 1 つも出ない
6. 93画面・ナビ・分類は同じ metadata から派生する
7. 動的 route の実 URL、選択中ナビ、パンくずを別々に解決する
8. 分類は 6 つ、境目は 5 つ（外側には付かない）
9. 見出しは分類の数だけ出る（線だけで済ませていない）
10. 境目のために要素を足していない（読み上げに区切りが増えない）

## `tests/ui/uiux-screen-single-purpose.test.ts` — normal / 99 件

主要な 8 件（残りは `it.each` の展開）:

1. 実在 route・route metadata・task manifest・priority map が 86 件で 1 対 1 になる
2. TSX から実行される action は全件 manifest にあり、manifest 外が 0 件
3. 同じ action の複数 route・複数 form 用途を畳まず、意味 entry 81 件を床固定する
4. screen 意味 entry はどの 1 件を削っても discovery との差分になる
5. screen mutation が所属する primary task は route ごとに 1 種類以下
6. 共通 Shell・read-only・UI demo も発見後に理由付きで分類する
7. runtime edge の module だけを移しても screen/mutation ID 集合は同じ
8. 末尾が delete の route が無い

## `tests/ui/uiux-spacing-and-copy.test.ts` — normal / 203 件

### A8 §0 上限の判定が端で切り替わる（`it.each` × 2 組）

説明文の長さ:
- ちょうど上限 → 通る
- 上限より 1 字長い → 落ちる
- 上限より 1 字短い → 通る

注意書きの個数:
- ちょうど上限 → 通る
- 上限より 1 個多い → 落ちる
- 上限より 1 個少ない → 通る

### A8 §1 余白が用途ごとに 1 つへ揃っている

- カード内の余白がすべて `--space-5`
- 生の数値で余白を書いていない

### A8 §2 画面の説明文が短い

`it.each(screens)` — **93 画面それぞれ**に 1 件。

### A8 §3 常に見えている注意書きが多すぎない

`it.each(screens)` — **93 画面それぞれ**に 1 件。

### A10 §1 落とすと決めた項目が消えている

- 落とす対象が 1 件以上ある（表そのものが空でない）
- 落とすと決めた画面の文章量が、決める前より減っている

### A10 §2 表が全画面を記録している

- 実在する画面がすべて表に載っている
- 表に載っている画面がすべて実在する
- どの行も 3 分類のどれかに 1 件以上を持つ

### 表が手書きの「今の値」を持たない

- 数を持つ節は `baseline_totals` だけ
- `baseline_totals` は数を持ったまま残っている
- `rules` の上限が、検査の使う値と一致している
- 今の値の在り処が書いてある

## `tests/ui/blog-ops-console-forms.test.tsx` — normal / 13 件

### 住所の行: 状態で押せるものが変わる

1. 配信できていない住所は「読者へ見せる」を選べない
2. 配信中の住所は「読者へ見せる」を選べる
3. いま見せている住所には、もう一度見せる操作を出さない
4. 取り下げ済みの住所には、取り下げの欄も押しボタンも出さない
5. 直近の失敗があれば、その文言を行に出す

### 断りは、名前を付けた欄のところに出る

6. 住所の形が違うという断りは、住所の欄に出る
7. 欄の名前が付かない断りは、フォームの結果として出る
8. 構えの断りは、その欄（名乗る名前）に出る

### すべての入力欄に、名札が結び付いている

9〜11. `it.each` × 3 欄

### キーボードで辿れる形になっている

12. 行に並ぶ 3 つの操作は、押しボタン自身が「どの操作か」を持つ
13. 操作は button であって、押せない要素に onclick を付けていない

## `tests/ui/blog-metrics-pages.test.tsx` — **a11y** / 18 件

1. 足切り未満だと、読み方の代わりに理由が出る
2. 足切りを越えると、読み方が出て理由は出ない
3. 記事の中の読まれ方も、根拠が足りなければ読み方を伏せる
4. 読者の行動: なぜ空なのかを本文で言う
5. 記事ごとの成果: なぜ空なのかを本文で言う
6. 記録はあるが成果の付いた記事が 1 本も無いときは、表を空で出さない
7. 数字が並んだ状態でも、円の表記が 1 つも出ない
8. ブログ全体では、記事の中の読まれ方を「出ない」と言う
9. 記事を選ぶと、どこまで届き、どこが押されたかが出る
10. 記事を選ぶと、幅の切替が出る
11. ブログ全体では、幅の切替を出さない
12. いま見ている幅は、押せる案内にしない
13. 選んだ幅が、そのまま読み口へ届く
14. 知らない幅を URL で渡されても、絞らずに描く

（残り 4 件は `it.each` の展開）

## `tests/ui/blog-ops-a11y-floor.test.tsx` — **a11y** / 7 件

### 受入 A14 — 名指しされた 6 つの読み上げ検査

1. 条文の 6 つは全部、経路の表に在る（総当たりの対象から落ちていない）
2〜7. `it.each` — 各画面に重大な読み上げの違反が無い

**1 件目が床。**

6 画面を総当たりする前に、
**6 画面が表に載っていること**を別に確かめる。

表から 1 画面が落ちれば、
残り 5 件は全部緑のまま通る。

## 境界のケース

| 何 | 境界 | どこ |
|---|---|---|
| 説明文 | 40 字 / 41 字 | `uiux-spacing-and-copy` §0 |
| 注意書き | 2 個 / 3 個 | `uiux-spacing-and-copy` §0 |
| 根拠の件数 | 30 件未満 / 30 件以上 | `blog-metrics-pages` 1〜2 |
| `viewport` | 既知の値 / 知らない値 | `blog-metrics-pages` 14 |
| 記事の選択 | 選んだ / 選ばない | `blog-metrics-pages` 8〜11 |
| 記録 | 0 件 / あるが該当 0 | `blog-metrics-pages` 5〜6 |
