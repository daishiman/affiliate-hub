# つなぎ目の呼び出し — 理由つきの除外の登録簿

判定は `node scripts/port-wiring.mjs`。一覧は `docs/product/port-wiring-report.md`（自動生成）。
**このファイルは人が書く。** 書くのは「意図的に呼んでいない」ことの理由だけ。

## この検査が生まれた経緯

「口はあるが誰も呼んでいない」が 3 回続いた。

| # | 何が | どうなっていたか | 画面からどう見えたか |
| --- | --- | --- | --- |
| 1 | 表現ポリシー | きまり 13 件を登録したが、照らす場所が無かった | 違反 0 件 |
| 2 | 読者ページの道具 | 8 つ載せたが、読者の権限では 1 つも動かなかった | 道具が並んでいる |
| 3 | 操作の記録 | 書き口があるのに、呼ぶ場所が 1 つも無かった | 記録 0 件 |

3 つとも**画面上は正常に見える**。これがこの穴の質の悪さで、
使ってみて気づくことができない。3 回続いたら偶然ではないので、
1 件ずつ潰すのをやめて機械で総ざらいすることにした。

**この検査の目的は「塞ぐこと」ではなく、「一覧が出て、それ以上増えないこと」である。**
いま呼ばれていない 82 件を今日全部塞ぐ必要は無い。
新しく足した口が黙って呼ばれないまま残ることを防げれば足りる。

## 数え方

- 呼び出し元として数えるのは `src/application` `src/presentation` `src/app` だけ
- **テストからの呼び出しは数えない。** 数えると全部緑になり、この検査自体が 4 件目の穴になる
- `src/infrastructure` も数えない。あそこに居るのは**実装**で、
  実装どうしの受け渡しを数えると「製品のどの流れからも使われていない」ものが緑になる
- 文字列の一致では数えない。ユースケースは枠の名前を付け替える
  （`contentVariants` を `variants` として受け取る等）ので、名前で探すと
  呼んでいるものを「呼んでいない」と数える。TypeScript の型検査に聞き、
  **その手続きがどのポートの宣言に由来するか**で数えている
- **ポート単位ではなく手続き単位。** ポート単位で「1 つでも呼ばれていれば OK」に
  すると、操作の記録のような**一部だけ埋まった状態**を見逃す

## 既存の検査との違い

`tests/presentation/composition-wiring.test.ts` は「入口が保存先の接続を渡しているか」を見る。
これは**つないだつもり**の検出で、価値はあるが守るものが違う。
操作の記録はあちらを通り抜けていた（接続は渡っていたが、誰も呼んでいなかった）。

## 除外の書き方

**理由が空の行は除外として認めない。** 「とりあえず除外」を積めなくするため。
除外の件数にも上限がある（`PORT_WIRING_MAX_EXCLUSIONS`）ので、
上限に当たったときに除外へ逃げることもできない。

| つなぎ目 | 理由 |
| --- | --- |

（現在 0 件。まだ 1 件も除外していない。）

## 2 つ目の検査 — 書き込みなのに操作の記録へ届いていない入口

上の総ざらいは**手続き単位で「1 回でも呼ばれたか」**しか見ない。
これでは足りないことが、4 件目で分かった。

`AuditLogPort.append` は**呼ばれている**（上の総ざらいでは緑）。
ところが実測すると、**書き込みをする入口 21 件のうち、記録へ届いているのは 1 件**
（記事の承認）だけだった。公開も、配信の予約も、成果の調整も、
リンクの取り込みも、記録されていない。

「口が塞がっている」ではなく「**一部の経路からしか通っていない**」形である。
呼ばれている以上、手続き単位で数える限り永久に緑のままになる。

### 数え方

- 入口は `src/application/usecases/` の `export function create〜UseCase`
- その入口から辿れるポートの呼び出しを集める。
  **同じファイルの補助関数も辿る**（`manage-content.ts` は `record()` を
  1 枚かぶせており、辿らないと「記録していない」に化ける）
- 手続きの名前の頭で、書き込みかどうかを判定する
- `AuditLogPort` 自身は書き込みに数えない。数えると、
  記録を書いただけの入口が自分で自分を満たしてしまう

### 判定できないものを 3 つ目として数える

書き込みかどうかは名前で決めるので、**語彙の決め方そのものが穴になりうる**。

- 「読み以外は全部書き」にすると、`newId` `signedUrl` `current` のような
  動詞でない手続きが書き込みに化ける（実測で 10 件ほど誤検出した）
- 「書き以外は全部読み」にすると、**並べ忘れた動詞が黙って漏れる**

どちらの片側も穴になるので、**両方並べ、どちらにも当たらないものを
「判定できない」として数える**。判定できないものは緑にも赤にもせず、
件数を固定するだけにする。語彙が増えたらそこが動くので、黙って漏れることがない。

### 除外の書き方（こちらは入口の名前で書く）

| 入口 | 理由 |
| --- | --- |

（現在 0 件。まだ 1 件も精査していない。）

読者の操作（気になる商品への追加、問い合わせ）のように、
運営者の操作記録を要求する理由が無いものは、精査のうえここへ移す。
移した分だけ `PORT_WIRING_MAX_UNRECORDED` を下げる
（**片方だけ動かすと合計が増える**ので、同じ変更の中で両方動かす）。

## 呼ばれていない 82 件の性質

自動生成の一覧を見ると、大半は**保存先や機能そのものがまだ無いもの**である。
販売店の価格・比較セット・サイトの設計図・ASP との接続・媒体への配信など、
残課題として正しく開いているものが多い。機能が乗るたびにこの数は減る。

ただし、次の 2 つは**性質が違う**ので先に書いておく。

- `ClickTrackingPort.recordClick` — 読者がリンクを押したことを 1 度も記録していない。
  成果の測定そのものが動いていないことになる
- `AuditLogPort.listByTarget` — 操作の記録を対象ごとに読み直す口が呼ばれていない。
  設定画面は `search` で全体を出すだけで、「この記事に何があったか」を辿れない

## 規範

- `quality-gates.config.mjs` の `PORT_WIRING_MAX_UNCALLED` / `PORT_WIRING_MAX_EXCLUSIONS`
  / `PORT_WIRING_MAX_UNRECORDED` / `PORT_WIRING_MAX_WRITE_EXCLUSIONS`
  / `PORT_WIRING_MAX_UNKNOWN_VERBS`
- `scripts/port-wiring.mjs`

## 赤の実測（2026-08-18）

**落ちない門は、無い門と見分けが付かない。** 3 つとも、わざと壊して赤を見た。

| 壊し方 | 結果 |
| --- | --- |
| `manage-content.ts` の `auditLog.append` を 1 つ外す | 呼ばれていない 82 → **83**、`AuditLogPort.append` を名指し、終了コード 1 |
| 承認の入口から `record()` を外す | 届いていない 20 → **21**、`createApproveContentUseCase` を名指し、終了コード 1 |
| 語彙から `submit` を 1 つ抜く | 判定できない 0 → **1**、`ContactPort.submit` を名指し、終了コード 1 |

このとき、**落ちた実行は報告書を上書きしない**ようにしてある。
上書きすると増えた 1 件が既知の一覧に混ざり、次回からは差が取れなくなる
（壊れた状態が「前回」として焼き付く）。
