/**
 * `server-only` の身代わり。
 *
 * 本物の `server-only` は、読み込まれた時点で例外を投げることで
 * 「この module をブラウザ向けの束に入れるな」を**ビルドの失敗**として知らせる。
 * 検査は Node で走るため、そのまま読むと検査のほうが落ちる。
 *
 * --- 守りを外していない ---
 * 身代わりを使うのは検査の実行時だけである（`vitest.config.mts` の別名指定）。
 * 実際の束を作るのは Next.js のビルドで、そこは本物を読む。
 * つまり「client から読んだら壊れる」性質はそのまま残っている。
 *
 * ここを空にする代わりに `import "server-only"` を消す、という直し方はしない。
 * 消すと守りそのものが無くなり、検査は通るのに束に秘密が混ざる状態になる。
 */
export {};
