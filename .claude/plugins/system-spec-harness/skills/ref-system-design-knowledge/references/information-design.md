# Information Design (画面情報設計) — deep knowledge card

> knowledge_id: `information-design` / status: `seed-example`。装飾を後工程の化粧と扱わず、利用文脈に応じた情報の取捨・優先順位・表現形式・意味関係の設計として扱う。

## 目的

画面へ載せる情報を利用文脈から逆算し、取捨・グループ化・優先順位付け・表示加工・表現形式を一体で決める。見栄えだけでなく、対象利用者が必要な情報と操作を発見し、正確に理解し、目的を完了できる情報構造を作る。

## 背景

保存項目をそのまま表へ並べて後から色・枠・アイコンを足すと、情報の存在は保証できても、何を先に見るかの判断を利用者へ丸投げする。一方でラベルや線を一律に削ってカードへ替えるだけでも、熟練者の一覧比較や支援技術が必要とする意味関係を失う。問題は「エンジニア対デザイナー」や「表対カード」ではなく、利用者の役割・熟練度・利用頻度・端末・情報量・誤操作コストに対して表現を選ぶ工程が欠けることである。設計と視覚表現は別工程ではなく、同じ判断を意味構造と見た目の両方へ写す作業である。

## 解決する問題

- データモデルの項目集合を表示モデルへ直写し、利用目的と無関係な項目まで同じ強さで並べる。
- 表・カード・リスト・グリッドを固定レパートリーまたは流行として選び、役割や反復頻度に合わない操作密度を強制する。
- ラベル・罫線・説明文を一律に付けるか一律に外し、意味の明示と冗長さを画面文脈で判定しない。
- 近接・整列・余白・区切り・アイコン・画像が共通の視覚文法を持たず、見た目の飾りと意味の手掛かりが競合する。
- 保存値をそのまま表示するか、表示層で独自計算し、正本値・表示値・利用者が読んだ意味が食い違う。
- 設計判断を静的な完成図だけで確定し、実利用での発見時間・完了・誤操作・回復を測らない。

## 中核概念

- **文脈プロファイル**: 少なくとも役割、熟練度、利用頻度、端末・入力手段、通常/最大付近の情報量、誤操作時の損失と回復可能性を記録する。職種名や画面種別だけから密度を決めない。
- **工程順序**: 文脈と達成タスク → 情報候補 → グループと優先度 → 削除仮説 → 表示モデル → 表現パターン → 配置と機能 → 意味を伝える視覚表現 → 実利用検証。表やカードの確定から始めない。
- **削除仮説**: 「ラベルを消す」を規則にせず、削除候補、代わりに意味を伝える手掛かり、誤読時の影響、検証方法、復元条件を持つ反証可能な仮説にする。意味が曖昧になる、誤操作コストが高い、検証できない場合は残す。
- **ラベルと意味関係**: 入力欄は placeholder やアイコンだけに依存せず、原則として見えるラベルを保ち、`label` と control、見出しと領域、説明/エラーと対象を意味的に関連付ける。表示専用データの見出しや反復ラベルは削除仮説の対象にできるが、`dl/dt/dd` や表見出し等の意味構造・関連付けは失わない。操作部品は可視ラベルの有無にかかわらず accessible name を持つ。
- **視覚文法**: 近接は同じまとまり、整列は比較軸と読み順、余白は階層と境界を示す。区切り線は余白だけでは境界が伝わらない場合に使う。アイコンは既知の意味を補助し、曖昧な意味をラベルの代わりにしない。画像は識別・比較・状態理解に寄与するとき強調し、装飾画像は情報階層を奪わない。色だけを唯一の意味伝達手段にしない。
- **表示優先度**: グループ間とグループ内の双方に優先度を付け、サイズ・太さ・色・余白・順序を design token の段階へ写す。すべてを強調しない一方、優先度の低さを到達不能と同義にしない。
- **表示モデル**: domain model (正本の意味と値) から view model (表記、単位、語彙、順序、集約、権限別可視性) への変換規則を明示する。金額・比率・状態などの業務値は正本側で導出し、表示層は勝手に再計算しない。
- **相対表現から正確値への到達**: 「明日」「一週間前」などは会話と走査を速めるが、正確な日時・タイムゾーンへ同じ文脈から到達できるようにする。tooltip だけに閉じず、詳細表示、補助テキスト、展開などキーボード/タッチ/支援技術でも使える手段を持たせる。監査・予約確定・順序比較では正確値を初期表示にできる。
- **表現パターンの open-world registry**: 表・カード・リスト・グリッドは例示であり上限付きの完全リストではない。patternごとに `pattern_id`、解く問題、適合/非適合タスク、文脈プロファイル、意味構造、variant、リスク、検証指標、根拠、鮮度を記録し、新しい複合/可視化/段階開示 pattern を発見・評価・統合できるようにする。類似 pattern は重複登録せず差分を variant にする。
- **固定数値ではなく実測**: 項目数、列数、カード数、文字数、余白量に汎用の固定上限を置かない。実データ分布、極端値、viewport、入力手段、性能、タスク成否を用いて breakpoint、密度、折返し、段階開示を決め、プロジェクト固有の閾値には根拠と再評価条件を付ける。
- **実利用フィードバック**: task completion、正答/誤操作、time-to-find/first-action、完了時間、取消/回復、検索・絞込・並替・展開・スクロールの利用、問い合わせを観測する。単一の平均値で確定せず、文脈プロファイル別の差、観察/インタビュー、アクセシビリティ確認を併用して pattern と削除仮説を更新する。

## 適用条件

- 人が情報を読み、判断し、入力または操作する画面・通知・ダッシュボード・対話フローを設計する。
- 新規画面だけでなく、情報は揃っているのに発見・理解・操作へ時間がかかる既存画面を改善する。
- 利用者と利用環境を完全には確定できなくても、未知を仮説として記録し、実データまたは利用観測で更新できる。
- design token や既存 component がある場合も、component 適用前に情報構造と意味関係を確定する。

## 非適用条件

- 機械だけが消費する API 応答・イベント・ログの契約は、このカードの視覚表現を直接適用せず、対応する契約/データ設計知識を使う。ただし人が読む管理ビューには適用する。
- 法令・監査・安全要件が表示内容と正確値を固定する場合、その情報を削除仮説に含めない。可読性は順序・グループ・段階開示・代替表現で改善する。
- pattern registry の候補に一次資料、適用条件、検証可能性が無い段階では、推奨 pattern として昇格せず探索候補に留める。

## トレードオフ・失敗モード

- 理解の速さと、熟練者の一覧比較・一括操作・高頻度処理は競合し得る。A/Bいずれかの様式を正解にせず、同じ情報の密度 variant や役割別 view を実測で選ぶ。
- ラベル削除を美観ルールとして適用し、フォームの見えるラベル、accessible name、項目間の意味関係、翻訳時の明確さを失う。
- 余白を増やせば読みやすいと決めつけ、大量反復操作のスクロール量と視線移動を増やす。逆に区切り線を増やし、すべての境界を同じ強さにする。
- アイコンだけで操作を表し、文化差・学習差・拡大表示・支援技術で意味が伝わらない。画像の強調が主情報を押し下げ、代替テキストや読込失敗時の情報を欠く。
- 相対時刻や短縮識別子を便利な表示として固定し、監査、同名識別、タイムゾーン境界、期限直前で正確値へ到達できなくする。
- 表示加工を業務値の導出まで広げ、複数 client で結果がずれる。逆に加工を避け、保存形式の解釈を利用者へ押し付ける。
- 固定上限を「ベストプラクティス」として全画面へ転用し、実データの長文、多言語、拡大表示、将来 pattern を排除する。
- analytics のクリック数だけを価値と読み替え、完了の質、誤操作、回復、非利用者、支援技術の困難を見落とす。計測のために個人情報を過剰収集しない。
- 情報設計の結論を完成画像か実装コードだけに残し、pattern選定、削除仮説、正確値への導線、検証結果を次の変更で再利用できない。

## 目的達成への寄与

- 文脈プロファイルと表示 model を要件へ接続し、レビューを「好み」ではなく、対象タスク・誤操作コスト・アクセシビリティとの整合検査に変える。
- 削除を可逆な仮説として扱うため、冗長さを減らしつつ、意味や安全性を失った場合に根拠を持って戻せる。
- 視覚文法と意味構造を同時に設計し、見た目、DOM/支援技術、操作結果の三者を一貫させる。
- open-world registry と実利用指標により、既存 pattern の機械適用では対応できない内容でも、発見・評価・再利用・鮮度監査の仕組みで改善を続けられる。

## ヒアリング・判断観点

- 利用者の役割・熟練度・利用頻度・端末/入力手段・情報量・誤操作コストはどう異なるか。同じ画面で両立するか、variant が必要か。
- 達成タスク、比較対象、判断に必要な正確さ、時間制約は何か。画面で優先するタスクと切替タスクは何か。
- 削除候補ごとに、意味を代替する手掛かり、誤読の影響、検証方法、復元条件はあるか。フォームの見えるラベルと意味関係を保っているか。
- 近接・整列・余白・区切り・アイコン・画像のそれぞれが伝える意味を説明できるか。装飾同士が競合していないか。
- domain model と view model の境界はどこか。相対値・短縮値・加工値から正確値と出典へ、すべての入力手段で到達できるか。
- 選んだ pattern は registry のどの条件に合い、どの反証で variant 変更または撤回するか。registry 外の候補を探索すべき gap は何か。
- 実利用のどの指標と定性観察で目的達成を判定し、どの文脈プロファイル別に再評価するか。

## 一次資料

- W3C, *Web Content Accessibility Guidelines (WCAG) 2.2* (W3C Recommendation, 2023-10-05), https://www.w3.org/TR/WCAG22/
- W3C WAI, *Labeling Controls — Web Accessibility Tutorials*, https://www.w3.org/WAI/tutorials/forms/labels/
- W3C WAI, *Understanding Success Criterion 1.3.1: Info and Relationships*, https://www.w3.org/WAI/WCAG22/Understanding/info-and-relationships.html
- Apple, *Human Interface Guidelines*, https://developer.apple.com/design/human-interface-guidelines/
- Edward R. Tufte, *The Visual Display of Quantitative Information*, 2nd ed. (Graphics Press, 2001), https://www.edwardtufte.com/book/the-visual-display-of-quantitative-information/

## 参考資料（二次資料）

- mskmiki, *なぜエンジニアが作る画面はダサいのか…?「理由」と「対策」を徹底解説【エンジニア向け画面デザイン講座】* (Qiita, updated 2024-12-25), https://qiita.com/mskmiki/items/544149987475719e417b 。利用文脈から情報を加工・強調する説明例として参照し、表/カードやラベル削除を普遍則にはしない。
- Jakob Nielsen, *Progressive Disclosure* (Nielsen Norman Group, 2006), https://www.nngroup.com/articles/progressive-disclosure/ 。段階開示の解説として参照し、適合条件と具体的な操作契約は一次資料および実利用検証で確定する。

## 鮮度

- class: `standard-tracked`
- last_checked: `2026-08-11`; review_by: `2027-02-11`
- trigger: WCAG/ARIA/主要 platform guideline の改訂、対象利用者・役割・端末・入力手段・データ分布・誤操作コストの変更、design token/component/pattern registry の改訂、相対/正確値の表記規則変更、実利用指標による仮説反証。
