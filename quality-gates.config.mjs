/**
 * 品質ゲートの唯一の正本。
 *
 * 閾値と検査項目をここ 1 箇所に集める。読み手は 6 つだけ:
 *   - `vitest.config.mts`              カバレッジ閾値と、段で絞った対象をそのまま渡す
 *   - `scripts/verify.mjs`             検査の並びと順番をそのまま実行する
 *   - `scripts/run-tests.mjs`          段で絞ってテストを走らせる
 *   - `scripts/tier-audit.mjs`         段の印が無いテストを見つける
 *   - `scripts/coverage-report.mjs`    層別の判定と記録の生成に使う
 *   - `tests/architecture/quality-gates.test.ts`  ここと CI が食い違っていないか見る
 *
 * **`.github/workflows/` に閾値や検査名を書かない。** 書いた瞬間に、
 * 手元と機械で別々の基準が育ち、「機械の上でだけ落ちる」状態が生まれる。
 *
 * 規範: docs/spec/11-CI-CD・品質ゲート仕様.md
 * 実測: docs/product/coverage.md
 */

/**
 * 全体の下限。要件は 80%。
 *
 * **下げて緑にすることを禁じる。** どうしても下げる場合は、
 * 下げた値・日付・理由を `docs/product/coverage.md` §4 に書いてから下げる。
 * 記録の無い引き下げは、次に見た人には「元からこの水準だった」としか見えない。
 */
export const GLOBAL_COVERAGE = {
  statements: 80,
  branches: 80,
  functions: 80,
  lines: 80,
};

/**
 * 層ごとの下限。
 *
 * 全体 1 個の数字は、どこが薄いかを隠す。
 * domain と application を高く置くのは、**壊れたときの被害が最も大きい**ため。
 * ここが薄いまま全体だけ 80% を超えても、守れているのは薄い場所だけになる。
 *
 * `target` に届くまでの経過は `current` に実測を書く（手で書かない。
 * `scripts/coverage-report.mjs` が更新する）。
 */
export const LAYER_COVERAGE = [
  {
    layer: "domain",
    dir: "src/domain",
    target: 90,
    why: "業務の決まりごと。壊れると仕様違反がそのまま本番に出る",
  },
  {
    layer: "application",
    dir: "src/application",
    target: 85,
    why: "手順。外側はポートで差し替えられるので、厚く書けない言い訳が無い",
  },
  {
    layer: "presentation",
    dir: "src/presentation",
    target: 75,
    why: "入口の配線。ここが外れると、正しいドメインが呼ばれないまま画面が動く",
  },
  {
    layer: "app",
    dir: "src/app",
    target: 70,
    why: "画面そのもの。4 つの状態と孤立ページを見る。見た目の網羅は追わない",
  },
  {
    layer: "infrastructure",
    dir: "src/infrastructure",
    target: 70,
    why: "道具。スタブが多く、実接続後に再評価する",
  },
];

/**
 * ミューテーションスコアの下限。
 *
 * 行カバレッジが答えていないことを、外から確かめるための数字である。
 * **コードをわざと壊して、テストが落ちるか**を見る。落ちなければ、
 * その場所には実質的にテストが無い（通っていただけ）。
 *
 * **`break` をゼロにしない。** ゼロにすると、走らせているだけで何も守らない状態になり、
 * 「ミューテーションテストがあります」という説明だけが残る。
 *
 * 対象は `src/domain` と `src/application` だけ（`stryker.config.mjs`）。
 * `infrastructure` / `presentation` / `app` は**対象外**で、その事実は
 * `docs/spec/10-テスト戦略仕様.md` §10 と `docs/product/mutation.md` に書く。
 * 覆っていない場所を書かずに「ミューテーションを入れた」と言わない。
 *
 * `break` は実測が出てから、その実測を下回らない位置に置く。
 * 実測より高く置くと初日から赤になり、下げる圧力がかかる。
 * 実測より大きく下げると、悪化しても気づけない。
 */
export const MUTATION_SCORE = {
  /** ここを超えていれば良い、の目安（判定には使わない） */
  high: 80,
  /** ここを割ったら薄い、の目安（判定には使わない） */
  low: 65,
  /**
   * **これを割ったら失敗**。実測に合わせて上げていく。下げるときは記録を残す。
   *
   * 2026-08-17 の実測は **67.20%**（domain + application、static 除外、
   * 10,028 変異、16 分 5 秒）。内訳と生き残りの中身は `docs/product/mutation.md`。
   * 実測の 2.2 ポイント下に置いてある。**これは緩めた数字ではない。**
   * 実測ぴったりに置くと、無関係な変更で分母が動いただけで赤くなり、
   * 「下げて緑にする」圧力が初日から掛かる。逆に大きく下げると悪化に気づけない。
   */
  break: 65,
};

/**
 * 2 段（PR）で変更範囲のミューテーションを走らせる上限ファイル数。
 *
 * **これは「重いから省く」ための数字ではない。置き場所を決める数字である。**
 * 実測で domain + application の全体（143 ファイル）に 12〜35 分かかった。
 * 15 分を目標にしている 2 段にそれを置くと、PR のたびに待たされる人が出て、
 * 遠からず「PR の門からミューテーションを外す」判断に流れる。
 * 外された検査は、遅い検査より悪い。
 *
 * これを超えた PR では 2 段で測らず、**3 段（夜間）の全体実測に回す**。
 * 検査そのものは消えていない。**測る場所が変わるだけ**で、
 * 遅くとも翌朝には同じコードが全体として測られる。
 *
 * 超えたことは必ず出力する。黙って見送ると「走った」と区別がつかない。
 */
export const MUTATION_MAX_CHANGED_FILES = 25;

/**
 * 「どの要件のために書いたのか分からないテスト」を許す上限（ファイル数）。
 *
 * 要件から書いたテストは、由来にする要件がある。
 * **実装を見てから書いたテストには、それが無い。**
 * 後者は「実装がそうなっているから、そう書いた」という循環で、
 * 実装が間違っていても緑になる。ここで数えているのはその循環の量である。
 *
 * 2026-08-17 の実測は **37 件 / 115 件**。
 * 起点は 39 件（性質テスト 5 件に `@req` を書いた時点）で、
 * 必須種別の宣言（§14）のために 7 ファイルへ `@req` と `@types` を足した結果 37 になった。
 * 残りは既存テストで、要件表の行にも `@req` 印にも出てこない。
 * 実測が下がったら追随させ、**増える方向だけを止める**。
 *
 * 一度に 0 にしないのは、残り 37 件へ機械的に印を付ける作業が
 * 「印を付けるための印」になりやすいためで、由来を確かめずに書いた `@req` は
 * この検査を無意味にする。減らすのは残課題として 1 件ずつ行う。
 *
 * **上げて緑にすることを禁じる。**
 *
 * → **28**（2026-08-18、残課題 70）。`tests/application/link-inbox.test.ts` に
 * `@req REQ-P02, REQ-S02` を書いて 1 件減り、実測が 28 になったので追随させた。
 * ここで 37 のまま置くと、**印が 9 件ぶん外れても緑のまま**になる。
 * この 1 件は「印を付けるための印」ではない — 受信箱の 4 状態の遷移と、
 * その遷移が誰の手で起きたかの記録を、ファイルの中で実際に見ていることを
 * 読んで確かめたうえで書いた。
 */
export const TRACEABILITY_MAX_UNLINKED = 28;

/**
 * テストの種別（語彙の正本）。
 *
 * 仕様 §13 の表と 1 対 1 に対応する。**ここに無い名前を印に書くと落ちる**ので、
 * 「境界値テスト」「境界チェック」のような表記ゆれで別種別に見える事故が起きない。
 */
export const TEST_TYPES = {
  equivalence: "等価分割",
  boundary: "境界値",
  "decision-table": "判定表",
  "state-transition": "状態遷移（許可 + 禁止）",
  pairwise: "ペアワイズ",
  scenario: "シナリオ",
  property: "性質（プロパティ）",
  mutation: "ミューテーション",
  contract: "API 契約（3 入口）",
  "db-migration": "DB マイグレーション往復",
  "db-constraint": "DB 制約",
  "db-concurrency": "DB 同時実行",
  "tenant-isolation": "テナント越境",
  "infra-config": "インフラ・設定",
  "screen-states": "画面 4 状態",
  e2e: "E2E（実ブラウザ）",
  keyboard: "キーボード・フォーカス",
  visual: "見た目の回帰",
  perf: "性能（N+1）",
  load: "負荷",
  idempotency: "冪等性",
  "fault-injection": "障害注入",
  a11y: "WCAG 2.2 AA",
  "permission-matrix": "権限マトリクス（できてはいけない側）",
  injection: "インジェクション",
  "prompt-injection": "プロンプトインジェクション",
  ssrf: "SSRF",
  secrets: "秘密情報の漏れ",
  redaction: "画面の写しの黒塗り",
  "dep-audit": "依存の監査",
  csrf: "CSRF",
  "rate-limit": "回数制限",
  "audit-log": "操作の記録",
  regression: "回帰（再現テスト）",
  "code-boundary": "構造の境界（置き場所・呼ぶ向き）",
};

/**
 * 要件の性質 → 必須のテスト種別（仕様 §14 の表の機械可読版）。
 *
 * 種別を決めない場合、テストは**書きやすいところから書かれる**。
 * その結果、難しい種別（権限のできてはいけない側・禁止された遷移）が永久に残る。
 * ここは「何を書くか」ではなく「**何を書かずに済ませられないか**」の表である。
 */
export const REQUIRED_TEST_TYPES = {
  "has-input": ["equivalence", "boundary"],
  "has-state": ["state-transition"],
  "has-permission": ["permission-matrix"],
  "has-tenant": ["tenant-isolation"],
  "has-external": ["fault-injection", "idempotency"],
  "has-screen": ["screen-states", "a11y", "keyboard"],
  "has-calculation": ["mutation", "boundary"],
  "has-ai-text": ["prompt-injection"],
  /*
   * 秘密の値（鍵・合言葉・トークン）を、保存・受け渡し・記録・表示のどれかで扱う。
   *
   * 足した理由。`secrets` という種別は最初から一覧にあったのに、**どの性質からも
   * 指されていなかった**。指されない種別は一度も要求されないので、
   * 名前があるだけで門としては無い。実際 `REQ-SEC10`（秘密をリポジトリに置かない）は
   * 性質を宣言できず、確かめた欄が `NOT RUN` のまま残っていた。
   *
   * 足したときに当たったのは宣言済み 36 件のうち `REQ-SEC09` の 1 件だけで、
   * そこは伏せ字の検査が既にあった（印だけが無かった）。
   * 残る `ssrf` / `db-migration` / `decision-table` / `contract` / `infra-config` /
   * `property` / `audit-log` の 7 つはまだ指されていない
   * （初めは 5 つと書いたが `contract` と `infra-config` を数え落としていた。
   * 2026-08-18 に訂正）。理由は `docs/product/required-test-types.md` §4 に書く。
   *
   * **この 7 つの並びは、上の 5 つについて古い。**
   * 同じ 2026-08-18 に `has-user-supplied-url`(→`ssrf`) /
   * `has-enumerated-input`(→`decision-table`) / `has-db-table` /
   * `has-runtime-config`(→`infra-config`) /
   * `has-recorded-operation`(→`audit-log`) が足されて、5 つは指されるようになった。
   * 残っているのは `contract` と `property` の 2 つである。
   *
   * ただし**「指されていない種別は 7 つ」という数え自体が最初から違う**。
   * 2026-08-19 に `TEST_TYPES` の全 34 種と `REQUIRED_TEST_TYPES` の指し先を
   * 突き合わせたところ、指されていないのは 16 種あった
   * （`pairwise` `scenario` `property` `contract` `db-constraint` `db-concurrency`
   * `e2e` `visual` `perf` `load` `injection` `redaction` `dep-audit` `csrf`
   * `rate-limit` `regression`）。元の 7 つは「このとき検討した範囲の中で」の数で、
   * 一覧全体を数えたものではなかった。
   * 手で書いた数字は、古くなっても古く見えない。数え直した日付を添えて残す。
   *
   * **上の「34 種 / 16 種」は、同じ 2026-08-19 のうちに古くなった。**
   * 数えたあとで `code-boundary`（`has-code-placement-rule` の指し先）が足されて
   * 一覧が 35 種になり、さらに `regression` が `has-known-breakage` から
   * 指されるようになった。**書いた時点では正しく、正しいまま偽になる**形なので、
   * 上の段は消さずに残し、ここに数え直しを重ねる。
   *
   * 数え直し（2026-08-19、上の段より後）:
   *   一覧          35 種
   *   指されている  20 種
   *   指されていない 15 種
   *     `pairwise` `scenario` `property` `contract` `db-constraint`
   *     `db-concurrency` `e2e` `visual` `perf` `load` `injection`
   *     `redaction` `dep-audit` `csrf` `rate-limit`
   *   一覧に無いのに指されている語  0 件
   *
   * **指されない種別は一度も要求されないので、名前があるだけで門としては
   * 存在していない。**しかも一覧を眺めた人には全部使われているように見える。
   * 読んで判断せず、突き合わせて数えること（残課題 78 ⑯）。
   */
  "has-secret": ["secrets"],
  /*
   * 外向きに取りに行く先が、**こちらが受け取った値で決まる**。
   *
   * 線の引き方をここに書いておく。「外部と通信する」では広すぎて、
   * 提供元が列挙されている LLM 呼び出しや、登録済みの ASP・配信先まで
   * 巻き込む。それらに `ssrf` を要求しても書きようが無く、除外理由が並ぶだけになる。
   * SSRF が成り立つ条件は**行き先を攻撃者が決められること**の 1 点なので、
   * 性質もそこだけを名指しする。
   *
   * 足す前に数えた（宣言済み 83 件のうち当たったのは 3 件）。
   *   REQ-SEC02 URL 取り込みの SSRF 対策       … `guarded-fetch` が実体
   *   REQ-P02   アフィリエイト URL 受信箱      … `link-ingestion` の `isInternalHost`
   *   REQ-A01   受け入れ条件 §30.1（URL 登録） … 同上
   * 当たらないと判断したもの: REQ-G11（提供元は列挙）/ REQ-P09（登録済み ASP）/
   * REQ-P08（登録済みの配信先）。いずれも行き先を利用者が決められない。
   *
   * まだ宣言表に無いが、宣言するときに当たるのが REQ-E13（`/go/[code]` の転送）。
   * 転送先は保存値だが、その保存値のもとは利用者が出した URL である。
   */
  "has-user-supplied-url": ["ssrf"],
  /*
   * 入力の軸が**すべて有限の列挙**で、大小・長短の端が無い。
   *
   * `has-input` との線引きは「**端があるか**」の 1 点だけにした。
   * 端があるなら `has-input`（等価分割＋境界値）、端が無いなら
   * こちら（等価分割＋判定表）。両方を名乗る要件は無い。
   * 線引きを決めずに `decision-table` を足すと `has-input` と重なり、
   * どちらを書いても片方が欠けたまま緑になる——それを避けるための 1 点である。
   *
   * `decision-table` を必須にしたのは、列挙で本当に困るのが
   * **数え落とし**だからである。3 つのうち 2 つだけ試しても等価分割としては
   * 成立してしまう。判定表は「全通りを表にして、埋まっていない行が無いこと」を
   * 名指しするので、列挙が増えたときに落ちる。
   *
   * これは逃げ道ではない。今までこの形の要件は
   * 「`has-input` を宣言して `boundary` を理由つき除外」で処理してきたが、
   * それは**端が無いことを毎回言い訳として書く**やり方で、
   * 数え落としのほうは誰も見ていなかった。除外が減り、要求は増える。
   */
  "has-enumerated-input": ["equivalence", "decision-table"],
  /*
   * その要件のデータが、**D1 の表として実在する**。
   *
   * 線の引き方。「保存する要件」ではなく「**表とマイグレーションを持つ要件**」に
   * した。見本データだけで動いているもの（`REQ-IM13` など）は表がまだ無いので
   * 当たらない。当ててしまうと、書きようの無い要求に対して除外理由が並ぶ。
   *
   * `db-migration` を必須にする理由は、ここで壊れると**静かだ**からである。
   * マイグレーションが表を作り損ねても、読み口が握り潰していれば
   * 画面には「0 件」と出る。「まだ何も無い」と見分けが付かない。
   * だから見るのは往復（書いて読み戻す）と、**表が無いときに失敗が返ること**の 2 つ。
   *
   * 足す前に数えた。`docs/product/traceability.md` で実装欄が
   * `infrastructure/persistence` を名指している要件は 267 行中 7 件
   * （`REQ-P08` / `REQ-P09` / `REQ-E13` / `REQ-SEC09` / `REQ-TM12` /
   * `REQ-TM13` / `REQ-IM13`）。うち宣言表に載っているのは 3 件で、
   * それがそのまま当たった先である。`REQ-TM12` は読み口の定義で表を持たない。
   * `REQ-E13` と `REQ-TM13` は宣言表に無いので、宣言するときに一緒に当たる
   * （どちらも検査は既にある。`d1-tracking-issuance` と `d1-telemetry`）。
   */
  "has-db-table": ["db-migration"],
  /*
   * 成り立つかどうかが、**実行環境に何が置かれているか**で決まる。
   * `wrangler.jsonc` の `vars` / binding / `wrangler secret` / 公開範囲がこれ。
   *
   * 線の引き方。「設定ファイルを読む要件」ではなく
   * 「**設定を間違えると、コードを 1 行も変えずに壊れる要件**」にした。
   * 前者だと D1 や KV を使う要件がほぼ全部入り、`db-migration` と重なる。
   *
   * `infra-config` を必須にする理由は、ここが**単体テストの外側**だからである。
   * 秘密を `vars` に書いてしまっても、型は通るし単体は緑のまま配られる。
   * 配ったあとに気づく形なので、置く場所そのものを検査するしかない。
   *
   * 足す前に数えた。宣言表 88 件のうち、実行環境の設定で成否が決まるのは
   * **`REQ-SEC10` の 1 件だけ**だった（`docs/product/traceability.md` を
   * `wrangler` / `binding` / `env.` / 環境変数 で引くと `REQ-CI05` / `REQ-CI07` /
   * `REQ-SEC10` / `REQ-TS07` の 4 件が出るが、`REQ-SEC10` 以外は未宣言）。
   * `REQ-SEC01`（テナント分離）には当てていない。印を持つ 2 ファイルが
   * `REQ-SEC01` を名乗ってはいるが、要件の文は分離の話で設定の話ではない。
   * **印が付いていることを、性質がある理由にしない。**
   *
   * ## 2026-08-19: 線を広げた（自動化の設定を含める）
   *
   * 上の線は `wrangler.jsonc` など**実行環境**の設定に寄って書いてあった。
   * CI/CD の設定（`.github/workflows/` と `quality-gates.config.mjs` 自身）も
   * **設定を間違えると、コードを 1 行も変えずに壊れる**に当たるので、
   * 線の内側へ入れる。名前を借りたのではなく、線の文をそのまま当てはめた。
   *
   * 壊れ方が同じであることが理由である。`ci.yml` から検査の呼び出しを消しても、
   * `nightly.yml` に定例を戻しても、`migrate.yml` の確認文字列を外しても、
   * **アプリのコードは 1 行も変わらず、単体テストは全部緑のまま**である。
   * 実行環境の設定と同じで、配ったあとに気づく形になる。
   *
   * 広げる前に測った（2026-08-19）。`.github/workflows/` の中身を 1 か所ずつ
   * 壊して赤くなるかを見たところ、**閾値の書き写し以外は何も見ていなかった**
   * （検査名の書き写し / 検査ステップの削除 / `ci.yml` の丸ごと上書き /
   * `deploy.yml` へのマイグレーション追加 / `migrate.yml` の確認文字列の削除、
   * いずれも緑）。この 5 つを塞いだのが `tests/architecture/ci-config.test.ts`。
   * **広げてから当てはまるものを探したのではなく、穴を測ってから広げている。**
   */
  "has-runtime-config": ["infra-config"],
  /*
   * あとから **「誰がやったか」を問われる操作**を起こす。
   *
   * 線の引き方。「保存する要件」でも「`auditLog.append` を呼ぶ要件」でもなく、
   * **`docs/spec/02-補充仕様-ギャップと追加要件.md` §7 が必須記録対象として
   * 列挙した 6 つ**（公開 / 削除 / リンク差し替え / 権限変更 / 成果データ修正 /
   * エクスポート）と、**要件の文が記録を明示的に求めているもの**にした。
   * 実装が呼んでいるかどうかを条件にすると、
   * **呼んでいないことが「性質が無い」ことの証拠になってしまう**
   * （呼んでいない要件こそ、この性質が捕まえたい相手である）。
   *
   * `audit-log` を必須にする理由は、**ここが既存の見張りの死角**だからである。
   * `scripts/port-wiring.mjs` は「保存先へ書く入口が記録へ届いているか」を
   * 構造から見ているが、見えるのは **`append` が呼ばれているかどうか**までで、
   * 中身は見えない。対象も、変更前後も、理由も空の記録は、
   * 一覧に 1 行増えるだけで**何も答えられない**。それを見るのはテストしかない。
   * さらに、**読み取りの操作は port-wiring に掛からない**
   * （実際にエクスポートがここから漏れていた。下の当てた先を見よ）。
   *
   * 足す前に数えた。宣言表 118 件のうち当たったのは 4 件で、
   * 新しく宣言した 3 件と合わせて 7 件が対象になる。
   *   REQ-P02  リンク差し替え     … `manage-link-inbox`（受け取り・差し替え・対象外）
   *   REQ-P08  公開・エクスポート … `publish-article` / `manage-distribution` /
   *                                 `publication-calendar` / `schedule-publication`
   *   REQ-P09  成果データ修正     … `manage-affiliate`（`conversion.adjusted`）
   *   REQ-SEC09 監査記録そのもの  … `d1-content`（実際の D1 へ書いて読み戻す）
   *   REQ-FB08 / REQ-FB09 / REQ-FB12 … 要件の文が「操作の記録」「誰が・どの鍵で」を求めている
   *
   * 当てなかったものと理由:
   *   REQ-P07  `site.created` を出しているが、**要件の文も §7 の列挙も求めていない**。
   *            **印が付いていることを、性質がある理由にしない。**
   *   REQ-R11  承認の記録は REQ-SEC09 が既に持っている。重ねると同じ検査を 2 回数える。
   *   REQ-P01  §7 の「権限変更」に当たるが、**書く側の実装がまだ無い**（残課題 62）。
   *            実装が来た日に当たる。いま当てても書きようが無い。
   *   REQ-SEC01 / REQ-SEC05  `manage-llm-credentials` が鍵の登録・失効を記録しているが、
   *            両要件の文はテナント分離とプロンプトインジェクションの話であって、
   *            記録の話ではない。
   *
   * 「削除」は当たる先が無い（削除のユースケースがコード全体に 1 つも無い）。
   */
  "has-recorded-operation": ["audit-log"],
  /*
   * **実装をどこに置いてよいか / どの向きに呼んでよいかが決まっている。**
   *
   * 足した理由。2026-08-19 の FD 群で、未宣言に残った 4 件がすべて同じ形だった。
   * 検査は実在して機能している（壊せば赤になる）のに、**付ける名前が無い**。
   * 入力でも状態でも権限でも画面でも計算でもない——守っているのは
   * 「値がどうなるか」ではなく「**コードがどこに書かれてよいか**」である。
   * 1 件ずつなら「仕方ない」で流れるが、4 件並んだ時点で語彙の不足だった（`ah-w9k`）。
   *
   * 線の引き方。「構造に関する要件」ではなく
   * 「**破り方が「別の場所にもう 1 つ書く」か「知らないはずの側を呼ぶ」の形をとる要件**」にした。
   * 前者だと層・ポート・命名の話がほぼ全部入り、`has-tenant` や `has-permission` と重なる。
   * 見分け方は、**その要件を破る差分が、値を 1 つも変えずに書けるか**である。
   * 書けるならこの性質が当たる（型検査も単体テストも緑のまま通る種類の壊れ方）。
   *
   * `code-boundary` を必須にする理由は、ここが**目視では守れない**からである。
   * 「domain が Drizzle を import していないか」を毎回のレビューで確認し続けることはできない。
   * そして破られたことは、値がずれる形では現れない。
   *
   * ## 種別の中身（`code-boundary` が要求すること）
   *
   * 1. **禁止された側を実際に書くと赤になる**こと。判定式をいじって測らない
   *    （2026-08-19 に 2 度、壊し方が測定対象に届かずに緑を見た。残課題 78 の天井②）
   * 2. **対象の集合が空でも赤になる**こと。しかも**集合ごとに**確かめること。
   *    この 2 つ目が、ほかの種別に無い要求である。理由は残課題 78 の 6 つ目に書いた:
   *    `dependency-direction.test.ts` には空振り防止が既にあったのに、それが守るのは
   *    `app/s` 側の別の集合だったので、`domain` を改名すると 13 件すべて緑になった。
   *    **構造の検査は対象をパスで拾うので、対象が消えたときに「違反 0 件」で緑になる。**
   *    値を見る検査には無い壊れ方なので、種別の側で名指ししておく。
   *
   * ## 足す前に数えた（2026-08-19、`docs/product/traceability.md` 全 241 件）
   *
   * 当たるのは 7 件。**7 件とも、禁止側を書いて赤になることを実測してから宣言した。**
   *   REQ-FD01  ランキング式の重複実装禁止   … 計算を domain/ranking の外に書く
   *   REQ-FD02  報酬と推薦の分離（構造の側） … domain/ranking から monetization を import
   *   REQ-FD05  テーブルの置き場所           … 決めた場所の外で sqliteTable / 再輸出を外す
   *   REQ-FD06  サーバー操作ファイルの形     … 非同期の関数以外を出す / 再輸出する
   *   REQ-TM12  計測は差し替え可能な接続部   … domain から infrastructure を import
   *   REQ-SEC02 取得の入口を 1 本に絞る       … guarded-fetch を通さず fetch を呼ぶ
   *   REQ-SEC04 編集評価と報酬データの分離   … ランキングのユースケースへ報酬ポートを注入
   *
   * `REQ-FD02` / `REQ-SEC02` / `REQ-SEC04` は既に別の性質を宣言している。
   * **横断で洗い直したときに当たったもので、FD 群のためだけの名前ではない**
   * **ただし件数は条件ではない。**当初 `ah-w9k` の受入条件に「4 件より多いこと」と書き、
   * 候補が 5 件見えた時点で「5 件より多い」へ引き上げたが、これは逆向きだった
   * （候補が条件を満たしそうになったから条件を上げるのは、
   * 「上限は下げる方向にしか動かさない」の裏返しの形である）。2026-08-19 に外した。
   * 名前を足してよいかは**検査が実在して機能しているか**で決まり、何件あるかでは決まらない。
   * 洗い直しは「FD 群のためだけの名前になっていないか」を見るために残す。
   *
   * 当てなかったものと理由:
   *   REQ-FD04  WebMCP でしか到達できない機能を作らない
   *             … 形は当たるが、**要件そのものを見ている検査がまだ無い**（残課題 88）。
   *               既存の検査は「4 入口へ同じ形で写っている」ことしか見ていないので、
   *               ここで名乗ると「見ていないもの」に名前が付く
   *   REQ-TS01  テストの土台を 1 箇所に集める
   *   REQ-TS03  外側はすべてポートのテストダブルに差し替える
   *             … どちらも形は当たるが、**判定欄が実装を指していて検査が無い**。
   *               検査を書く前に名前だけ足すのは、数を下げるための語彙になる
   *
   * 2026-08-19 追記。この表は 2 か所が古くなった。
   *   - `REQ-TS01` は**検査を書いてから**宣言した
   *     （`tests/architecture/test-foundation.test.ts`）。当てなかった側から外れる
   *   - `REQ-TS09`（契約検査）が 8 件目として当たった。名前を足した時点で当たっていたのに、
   *     `docs/product/required-test-types.md` の側に「`contract` 単独の性質が無いので
   *     宣言できない」と書いたまま残っていた。**理由のほうが先に古くなる**
   */
  "has-code-placement-rule": ["code-boundary"],
  /*
   * **配色と明暗の組み合わせを持つ。どの組み合わせでも読めなければならない。**
   *
   * 足した理由。2026-08-19 に `REQ-TS06`（読み上げ検査と、配色 5 種 × 明暗 2 種の
   * コントラスト）を点検したところ、検査は実在して機能していた
   * （`tests/ui/theme-contrast.test.ts` が配色 10 種 × 明暗 2 種の 20 通りを
   * 登録表から総当たりし、空振り防止も 2 件持っている）。**付ける名前が無いだけだった。**
   *
   * 線の引き方。「見た目に関する要件」ではなく
   * 「**配色・明暗の組み合わせそのものを持っていて、増えたぶんが自動で検査に入らなければ
   * ならない要件**」にした。
   *
   * ## `has-screen` の代わりに使わせないための線
   *
   * この性質は `a11y` しか要求しない。`has-screen`（`screen-states` / `a11y` /
   * `keyboard`）より**軽い**ので、放っておくと画面の要件がこちらへ逃げる道になる。
   * そうならないのは、**当たるのが「画面」ではなく「組み合わせの集合を持っている側」**
   * だからである。ふつうの画面は配色の一覧を持たない（`REQ-TH03` のとおり、
   * 切り替え部品は選択肢を渡してもらう形で、自分では持たない）。
   * **画面に当てたくなったら、それは `has-screen` である。**
   *
   * ## 種別の中身（`a11y` がここで要求すること）
   *
   * 1. 組み合わせを**登録表から取る**こと。テストに配色名を書き並べると、
   *    増えた 1 つは検査に入らないまま緑になる（`REQ-TH05` が言っているのはこれ）
   * 2. **対象が 0 件でも赤になる**こと。総当たりは対象が空だと全部合格になる
   *
   * ## 足す前に数えた（2026-08-19、`docs/product/traceability.md` 全 241 件）
   *
   * 当たるのは 3 件。**3 件とも壊して赤を実測してから宣言した。**
   *   REQ-TS06  読み上げ検査とコントラスト … 配色を 1 つ足してトークンを書かない
   *   REQ-TH01  明暗を切り替えられる       … 暗い側を明るい側の値のままにする
   *   REQ-TH02  配色を複数から選べる       … 名札だけ足してトークンを書き忘れる
   *
   * `REQ-TH01` / `REQ-TH02` は既に別の性質を宣言している
   * （横断で洗い直したときに当たったもので、`REQ-TS06` のためだけの名前ではない。
   * ただし**件数は条件ではない**。理由は 1 つ上の性質の欄に書いた）。
   *
   * 当てなかったものと理由:
   *   REQ-TH03  切り替え部品の一元化
   *             … 組み合わせを持っていない側の要件である（持たないことが要件）。
   *               破り方は「別の場所にもう 1 つ書く」なので `has-code-placement-rule` の形。
   *               ただしいまの検査は「見本帳に載っているか」しか見ていないので、
   *               名乗ると見ていないものに名前が付く。未宣言のまま残す
   *   REQ-TH05  配色を増やすと自動で検査に入る
   *             … この性質の「種別の中身」1 番そのものだが、**製品の振る舞いではなく
   *               検査の仕組みについての要件**である。`a11y` を名乗ると、
   *               コントラストを見ていることが自動追随の証拠に化ける。未宣言のまま残す
   */
  "has-color-scheme-variants": ["a11y"],
  /*
   * **一度実際に壊れていて、生成をやり直すと同じ形で戻ってくる。**
   *
   * 足した理由。`regression`（回帰・再現テスト）は一覧に最初からあるのに、
   * **どの性質からも指されていなかった**（`TEST_TYPES` の指されていない 16 種の 1 つ）。
   * 指されない種別は一度も要求されないので、名前があるだけで門としては無い。
   *
   * 線の引き方。「壊れたら困る要件」ではない——それでは全部が当たる。
   * 当てるのは次の 2 つが**両方**そろっているものだけにした。
   *   1. **実際に壊れた事実がある**（想定ではなく、壊れているところを見た）
   *   2. **直しても原因が残っている**。生成物なら生成元が、手順なら手順が、
   *      直っていない。つまり同じ形が戻ってくる道が開いたままである
   * 2 が効く。原因まで直したものは、戻ってくる道が無いので回帰テストではなく
   * ふつうの検査になる。ここで要求しているのは「**戻ってきたときに知らせる**」ことで、
   * 「壊れていない」ことではない。
   *
   * ## 足す前に数えた（2026-08-19、`docs/product/traceability.md` 全 242 件）
   *
   * 当たるのは 1 件だけだった。
   *   REQ-TS11  仕様章のコード塊が本文を飲み込んでいないこと
   *             … `system-spec/backend.md` が実際に 169 行を飲み込んでいた（1）。
   *               章は直したが、生成元の `spec-state.json` の `qa_log` は
   *               閉じフェンスだけを持ったままで、compile を走らせ直すと戻る（2）。
   *
   * 「回帰」「再発」「再現」で要件表を引いて出た 2 件（`REQ-S08` / `REQ-CI01`）は
   * 当てなかった。前者は絞込の軸の話、後者は CI と手元の再現性の話で、
   * どちらも「一度壊れた」に当たらない。**言葉が同じでも性質は別である。**
   *
   * これは `bd remember` の「塞げない穴は検査として書く」と同じ形の道具だが、
   * 向きが違う。あちらは**塞げない**ものを監視する（塞がった日に赤くなる）。
   * こちらは**塞いだが原因が残っている**ものを監視する（戻ってきた日に赤くなる）。
   */
  "has-known-breakage": ["regression"],
};

/**
 * 必須種別を宣言していない要件を許す上限（件数）。
 *
 * 要件表には 240 件の要件 ID がある。全部に性質と必須種別を宣言し切るまで
 * 検査を入れない、という順にすると**検査は永久に入らない**。
 * そこで `TRACEABILITY_MAX_UNLINKED` と同じ形にし、
 * **新しく足す要件には最初から宣言を要求し、既存の未宣言は増やせない**ようにする。
 *
 * 2026-08-17 の実測は **205 件 / 241 件**（宣言済 36 件）。
 * この数字は「未宣言が 205 件ある」という事実の記録であって、目標ではない。
 *
 * 経緯: 228（宣言済 13）→ 205（宣言済 36）。権限・テナント・セキュリティの 24 件のうち
 * 23 件を宣言した（`REQ-SEC10` だけは、性質の語彙に当てはまるものが無く保留）。
 *
 * → **204**（宣言済 37）。保留していた `REQ-SEC10` を、`has-secret` を語彙へ足して宣言した。
 * 数の上では 1 件しか動かないが、この 1 件のために
 * `tests/architecture/secrets-not-in-repo.test.ts` を新しく書いている
 * （それまで確かめた欄は `NOT RUN` で、証拠は `.gitignore` の 1 行だけだった）。
 *
 * → **196**（宣言済 45）。受け入れ条件 `REQ-A01`〜`REQ-A08` の 8 件を宣言した。
 * この 8 件は「依頼者が受け取ったときに確かめる操作」で、検証は
 * `tests/acceptance/acceptance-criteria.test.ts` が入口から 1 本ずつ通している。
 * ただし**入口を 1 本通すことと、必須種別を満たすことは別**である。
 * 受け入れ用の検査には分かれ目（境界・状態の全遷移・権限の表）が無いので、
 * 印は受け入れ用の検査ではなく、**その分かれ目を実際に持っている
 * 単体側の検査**へ付けた（`link-ingestion` / `read-product` / `manage-personas` /
 * `generation-matrix` / `generation-plan` / `build-site` /
 * `manage-distribution` / `affiliate`）。
 * 経緯と、性質を 2 つ取り下げた理由は
 * `docs/product/required-test-types.md` §4 に書いた。
 *
 * → **168**（宣言済 73）。生成基盤・入口・出来事の 28 件
 * （`REQ-G01`〜`REQ-G11` / `REQ-API01` / `REQ-EV01`〜`REQ-EV16`）を宣言した（`ah-29w`）。
 * 印を付けるために**検査を 3 つ足している**。足さずに済ませると、
 * 次の 3 つがどれも「1 件だけ試して 16 件ぶん緑」になっていた。
 *
 *   1. 出来事 16 件の必須項目 … それまで 1 件（`content_variant.approved`）しか
 *      落として試していなかった。16 件すべてで、必須のうち 1 つだけ欠けた形を試す
 *   2. ローンチ基準の門 … `canActivatePromptVersion()` が `false` を返すことしか
 *      見ておらず、**常に false を返す関数でも通った**。開く側も見る
 *   3. 生成の依頼の同一性 … 同じ入力から同じ依頼が出て行くことを誰も見ていなかった
 *
 * 性質の当て方は `docs/product/required-test-types.md` §4 に 1 件ずつ書いた。
 * 理由つき除外は 1 件も増やしていない（10 件のまま）。
 *
 * → **158**（宣言済 83）。WebMCP・バックエンド MCP の 15 件のうち **10 件**を宣言した
 * （`REQ-M01`〜`REQ-M03` / `REQ-WA01` `REQ-WA02` / `REQ-WB01` `REQ-WB02` /
 * `REQ-WC02` `REQ-WC04` `REQ-WC06`。`ah-44d`）。
 * ここでも印だけでは済まず、`tests/presentation/webmcp-registration.test.ts` を
 * 新しく書いている。`resolveModelContext()` と `registerWebMcpTools()` には
 * 検査が 1 つも無かった（React の部品の中にあり、画面を描かないと触れなかった）。
 * 正規の経路を先に見ることも、離脱時に道具を渡し直して空にすることも、
 * 壊しても誰も落ちない状態だった。
 *
 * → **153**（宣言済 88）。残っていた 5 件（`REQ-WC01` `REQ-WC03` `REQ-WC05`
 * `REQ-WC07` `REQ-WC08`）を宣言した（`ah-44d` 完了）。
 * この 5 件は「入力が列挙で `boundary` の当てどころが無い」ため、
 * 慣行どおりなら `has-input` を宣言して `boundary` を理由つき除外にする形で、
 * 除外が 10 → 15 になるはずだった。除外の上限は動かさず、
 * 代わりに性質 `has-enumerated-input`（等価分割＋判定表）を語彙へ足した。
 *
 * **除外は増えるどころか 10 → 7 に減った。** 同じ形の要件が 2 件
 * （`REQ-SEC06` `REQ-SEC07`）と、`REQ-P02` の `boundary`（新しい印が付いて
 * 除外の理由が事実と食い違った）が、除外を書かずに済むようになったためである。
 *
 * 印だけでは済まなかった。判定表にそろえた結果、**4 か所で数え落としが見つかった**。
 *
 *   1. `REQ-WC01` 登録先の選び方 … 条件 2 つ＝ 4 通りのうち 3 通りしか無く、
 *      「新しい経路だけがある」が抜けていた（実装は正しかったが、壊れても落ちない）
 *   2. `REQ-WC03` 機能フラグ … 止める値の一覧を検査側へ書き写しており、
 *      実装にある `disabled` が試されていなかった
 *   3. `REQ-WC07` エラー形式 … `statusOf()` も `errorToMcpResult()` も
 *      `tests/` のどこからも呼ばれておらず、**番号の表を丸ごと消しても落ちなかった**
 *   4. `REQ-WC08` 旧 3 ツール … 「移行済み」としか書かれておらず、
 *      `record_conversion` に代わる書き込みの口が 1 つも無いことが見えていなかった
 *
 * → **123**（宣言済 118）。画面の要件 30 件を宣言した（`ah-cry`）。
 * 読者側 `REQ-B01`〜`B18`、§22 の画面仕様 `REQ-S01`〜`S10`、
 * 外観の選択 `REQ-TH02` `REQ-TH03`。
 *
 * ここは**印を足すだけで済んだ数少ない回**である。理由を書いておく。
 * 画面の検査は最初から `tests/ui/route-table.ts` の表を総当たりする作りになっていて、
 * 読者側 20 本・運営側 33 本が既に 1 枚ずつ描かれ、a11y も
 * キーボードの辿り方も見られていた。**足りなかったのは検査ではなく、
 * その検査がどの要件のものかという結び付けだけ**だった。
 * 逆に言えば、画面を 1 枚足したときに要件との結びが自動で付くわけではない。
 *
 * `REQ-TH04`（チラつきを起こさない）と `REQ-TH05`（配色を増やすと自動で検査に入る）は
 * 宣言していない。理由は `docs/product/required-test-types.md` §4 に書いた。
 * **32 件のうち 30 件、が正しい数字である。**
 *
 * → **120**（宣言済 121）。記録の義務がある要件 3 件（`REQ-FB08` / `REQ-FB09` /
 * `REQ-FB12`）を宣言し、既に宣言済みの 4 件（`REQ-P02` / `REQ-P08` / `REQ-P09` /
 * `REQ-SEC09`）へ `has-recorded-operation` を足した（残課題 69）。
 * この回は**印を足すだけでは済まなかった** — 性質を当てた `REQ-P08` に、
 * **記録されていない持ち出し**（手作業での書き出し）が 1 つ含まれていた。
 * 読み取りの操作なので `scripts/port-wiring.mjs` の見張りには掛からず、
 * `AuditAction` に `export.performed` という語だけが**使われないまま**あった。
 * 記録を足して、渡す前に断るようにしてある。
 *
 * 2026-08-19 に 120 → 109。記事の品質 11 件（`REQ-QC01`〜`REQ-QC11`）を宣言した。
 * この回も印を足すだけでは済まなかった — 一覧（誇大表現 8 / 見出し 8 /
 * 相対的な日付 11 / 単位を付ける語 13 / 一人称の言い回し 6）が、
 * **どれも 1 行だけ試されていた**。一覧の全行に当てる検査を書いてから宣言している
 * （`tests/domain/quality-check-tables.test.ts`）。
 *
 * 2026-08-19 に 109 → 100。書き方の決めごと 9 件（`REQ-W01`〜`W05` / `W07` /
 * `W09` / `W10` / `W12`）を宣言した。この回も印を足すだけでは済まなかった —
 * 記事の型ごとの節 22 個のうち、名指しされていたのは 6 個だけで、
 * **1 つを詳しく見る記事・比べる記事・道具のページは 0 個**だった。
 * 「検証条件」「差分表」「計算・判定の根拠」を丸ごと消しても、
 * 既存の 33 件は全部緑のまま通った（実測）。5 つの型ぜんぶに当てる検査を
 * 書いてから宣言している（`tests/domain/article-type-sections.test.ts`）。
 * `REQ-W06` / `W08` / `W11` は宣言していない。理由は
 * `docs/product/required-test-types.md` §4。
 *
 * 2026-08-19 に 100 → 95。計測 5 件（`REQ-TM01` / `TM04` / `TM07` / `TM08` /
 * `TM09`）を宣言した。ここでも印を足すだけでは済まなかった —
 * 計測イベント 12 件のうち `search_performed` と `filter_changed` は
 * **テストのどこにも出ておらず、表から消しても 3810 件すべてが緑だった**（実測）。
 * 禁止語 17 のうち、実際に送って落ちることを確かめてあったのは 3 語。
 * 全行に当てる検査を書いてから宣言している
 * （`tests/domain/telemetry-tables.test.ts`）。
 *
 *
 * 2026-08-19 に 95 → 83。エンティティ 12 件（`REQ-E02` / `E05` / `E09` / `E10` /
 * `E11` / `E13` / `E15` / `E22` / `E23` / `E24` / `E26` / `E30`）を宣言した。
 * ここで出たのは前の 2 回とは別の形で、**断る側だけが確かめられていない**。
 * `E09` / `E10` / `E11` / `E15` は「実装済」だが作る関数を直接呼ぶテストが 1 つも無く、
 * 見本データが正しい値で 1 回ずつ呼ぶだけだった。**断り 11 か所を消して
 * 全部走らせても 3875 件すべてが緑だった**（実測）。
 * 「使われている」と「確かめられている」は別である。
 * `has-input` を名乗るために、既にあった等価分割へ**端**を足している
 * （期間の端・必要件数の端・許諾の期限の端・識別子 0/1 件・200/201 文字）。
 *
 *
 * 2026-08-19 に 83 → 79。テスト戦略 4 件（`REQ-TS04` / `TS05` / `TS07` / `TS08`）を
 * 宣言した。この群は**残り 6 件を宣言しないことのほうが結論**である
 * （`TS01` / `TS02` / `TS03` / `TS06` / `TS09` / `TS10`。理由は
 * `docs/product/required-test-types.md` §4）。
 * `TS02` / `TS03` は「その群のテストが存在すること」を言うメタ要件で、
 * 実装の側に当たる性質を持たない。**性質が無いものに性質の名前を借りない。**
 * この回に見つかったのは判定欄の側で、`TS01` の「土台自身は契約検査で
 * 『各テストが自前で組み立てていないこと』を見る」は**その検査が存在しない**
 * （`tests/architecture/` 15 ファイルの `describe` を全部読んで確認）。
 * `TS04` の「368 件」は実測 463 件、`TS08` の「121 件」は実測 122 件で、
 * どちらも手で書いた数字が古くなっていた。
 *
 *
 * 2026-08-19 に 79 → 68。改善ループ 11 件（`REQ-IM01`〜`IM04` / `IM06`〜`IM12`）を
 * 宣言した。ここで出たのは**期待値を実装から作る**形で、前の 3 回と別である。
 * 「調整してはいけないものは、どれも軸にできない」という検査は
 * `NON_OPTIMIZABLE` を回して `NON_OPTIMIZABLE` 由来の禁止を確かめていた。
 * 一覧から 1 件消えると、残りを回して残りぶん確かめ、**緑のまま通る**。
 * 一覧の 37 項目を 1 件ずつ書き換えて測ったところ **32 件が緑**だった
 * （調整禁止 6/6・外せない約束 5/5・改善の軸 17/20・未実装ループ 4/4）。
 * 重いのは調整禁止で、そこから「広告であることの表示」が消えると
 * **それを A/B 試験の軸にできる**のに、誰も落ちなかった。
 * 一覧をテスト側の文字列で固定してから宣言している（37/37 が赤になることを実測）。
 * 判定欄の嘘は `REQ-IM10`（ループ 5 種・動くのは 1 種類 → 実測 6 種・2 種類）。
 *
 *
 *
 * 2026-08-19 に 68 → 49。データモデル 19 件（`REQ-E01` / `E03` / `E04` / `E06` /
 * `E07` / `E08` / `E12` / `E14` / `E17`〜`E21` / `E25` / `E27`〜`E29` / `E31` / `E32`）を
 * 宣言した。この群は前回（95 → 83）に見つけた「入口が 1 つしかなく、そこを通る値が
 * いつも正しい」の続きで、今回は**残り 18 ファイルの断り 76 か所を 1 か所ずつ
 * `if (false)` に変えて測った**。**11 か所は 3960 件すべてが緑のまま**だった
 * （成果リンクの URL 空 / 主張の文が空 / 確認者が空 / 本文が空 / 点数 3 つの範囲 /
 * プロンプト版が空 / 人の承認なしでの承認 / ルール名が空 / 検出表現が空 /
 * 分野の語彙 / 出力先の語彙）。
 * 赤くなった 65 か所も「断るか」しか見ていないものが多く、**端は測られていなかった**。
 * 判定欄の嘘は要件表 F 節の末尾で、「不変条件は 2 ファイルが機械で確かめている」と
 * 集合について書いてあったが、上の 11 か所がその外にあった。
 * `REQ-E16`（ProductVariant）は宣言していない。作る関数が無く、断る場所が
 * 1 つも無いため、当てる先が存在しない。**除外の枠へ回さず未宣言に残す。**
 *
 * ## 2026-08-19（38 → 29）: 改善要望の受け口 9 件
 *
 * 残り 38 件の内訳は **TM 8 / FD 6 / TS 6 / FB 9 / W 3 / CI 2 / TH 2 / E 1 / IM 1**。
 * 最大の群は FB（利用者からの改善要望を受け取る仕組み）で、9 件すべてを宣言した。
 * 性質は画面 5 / 入力 2 / 列挙 2 / 権限 1 / 鍵 1 / AI 文 1。当てたのは
 * `tests/ui/feedback-button.test.tsx`・`tests/ui/capture-canvas.test.tsx`・
 * `tests/domain/loop-kinds.test.ts`・`tests/domain/handoff-prompt.test.ts`・
 * `tests/domain/feedback.test.ts`・`tests/ui/page-render.test.tsx`・
 * `tests/ui/keyboard-operation.test.tsx`。**印を付けただけの件は 1 件も無い。**
 *
 * 宣言する前に 19 通り壊して測り、直したうえで 19/19 が赤になる。
 * **端は別に当てた**（本文 4000 / 「どうなってほしいか」200 / 画像 4MiB /
 * 画像の種類 / 保存 180 日の 7 通り）。うち **2 つは最初みどりだった**。
 * 保存日数は境目を定数から作っていたので**定数を動かすと境目も一緒に動き**、
 * 180 が 179 になっても通った。「どうなってほしいか」の上限は誰も見ていなかった。
 * どちらも数そのものを名指しする形で塞ぎ、測り直して赤にしてある。
 *
 * 書いたばかりの検査の穴も 2 つ見つかった。禁止語の表を一覧から作ると、
 * **一覧が縮んだ日に行も一緒に消えて緑になる**（名指しの 1 行を足した）。
 * axe は名前の無い `role="group"` を違反として上げないので、
 * 読み上げの検査だけでは道具の並びから名前が消えたことに気づけない
 * （`getByRole("group", { name })` を足した）。これは
 * **壊しても赤にならない理由が「守られていないから」ではなく
 * 「壊し方が測定対象に届いていないから」**の 3 例目である。
 *
 * 判定欄の嘘が 1 つ。`REQ-FB03` に「Esc で閉じる / フォーカスを閉じ込める」と
 * 書いてあったが、実装にどちらも無かった。**文章を直さず実装で塞いだ**
 * （`presentation/ui/patterns/feedback-button.tsx`）。`REQ-CI01` と同じ形で、
 * 判定欄の点検は「破れている要件を見つける作業」になる。
 *
 * 残り 29 件（TM 8 / FD 6 / TS 6 / W 3 / CI 2 / TH 2 / E 1 / IM 1）は次の回。
 *
 * ## 2026-08-19（49 → 38）: CI/CD の設定 11 件
 *
 * 49 件の内訳を数えたところ **CI 13 / FB 9 / TM 8 / FD 6 / TS 6 / W 3 / TH 2 /
 * E 1 / IM 1** で、最大の群が CI（自動化と公開）だった。
 * 性質は `has-runtime-config` → `infra-config`。当てたのは
 * `tests/architecture/ci-config.test.ts`（21 件）で、宣言のために新しく書いている。
 * **印を付けただけの件は 1 件も無い。**
 *
 * 宣言する前に `.github/workflows/` を 1 か所ずつ壊して測った（20 通り）。
 * 前回みどりだった 5 つの穴（検査名の書き写し / 検査ステップの削除 /
 * `ci.yml` の丸ごと上書き / `deploy.yml` へのマイグレーション追加 /
 * `migrate.yml` の確認文字列の削除）は、いま全部赤くなる。
 * **端は別に当てた**（AI 上限をちょうど 51 なら通し 52 と 0 で止める /
 * 控えの保管日数 30 を 29 にすると止める / 待ち時間の 2 回を 1 回に減らすと止める）。
 *
 * 測ったことで、書いたばかりの検査の穴も 2 つ見つかった。
 * `exit 1` があることだけを見ると、**判定の枝から消しても別の `exit 1` で緑になる**。
 * 控えの順番も、名前を含むかどうかでは動かしたことにならない。どちらも直してある。
 * **緑は、書いた本人にとっても証拠にならない。**
 *
 * 残した 2 件。`REQ-CI08` は文書のため当てどころが実装に無い。
 * `REQ-CI12`（目標時間の超過は警告であって落とさない）は、超過時の挙動を測るのに
 * 時間を外から渡せる作りが要るが、`scripts/verify.mjs` は受け取れない。
 * **「除外の枠が満杯だから書けない」ではなく、「実装が時間を受け取れるようになれば
 * 宣言できる」**。除外へ回さず未宣言に残す。
 *
 * **上げて緑にすることを禁じる。**
 *
 * 2026-08-19: 29 → 22。計測（TM）7 件を宣言した
 * （TM02 / TM03 / TM05 / TM06 / TM10 / TM11 / TM13）。
 * 宣言する前に 29 通り壊して測ったところ、**12 通りが緑のまま通った**。
 * 追跡表の判定欄が指していた検査は、TM05 では**引用した名前が存在せず**、
 * TM06 と TM10 では**別のことを見ている検査**だった。塞いでから宣言した。
 *
 * 残した 1 件。`REQ-TM12`（計測は差し替え可能な接続部にする＝層の分離）は、
 * 当たる性質が語彙に無い。`tests/architecture/dependency-direction.test.ts` は
 * 実在して機能しているが、**その検査に名前が無い**。
 * 除外の枠（7/7 満杯）へ回さず、未宣言に残す。
 *
 * 2026-08-19: 22 → 20。禁止依存（FD）6 件のうち **2 件だけ**宣言した
 * （`REQ-FD02` 商業データの遮断 / `REQ-FD03` 根拠のない主張）。
 * 判定欄を **29 通り壊して測り、8 通りが緑のまま通った**。
 *
 * 残した 4 件（`REQ-FD01` / `REQ-FD04` / `REQ-FD05` / `REQ-FD06`）は、
 * **当たる性質が語彙に無い**。どれも「実装をどこに置いてよいか」の禁止であって、
 * 入力・状態・権限・画面・計算のどれでもない。検査は 4 件とも実在し、
 * この回に穴を 3 つ塞いでもいる（層の空振り・再輸出・テーブルの置き場所）。
 * **満たしていない種別の名前を借りないため、未宣言に残す。**
 * `REQ-TM12` と同じ理由なので、語彙の不足（`ah-w9k`）は 4 件から 8 件になった。
 *
 * 2026-08-19: 20 → 16。**語彙のほうを足して解いた**（`ah-w9k` を実装に移した）。
 * `has-code-placement-rule` → `code-boundary` を足し、`REQ-FD01` / `REQ-FD05` /
 * `REQ-FD06` / `REQ-TM12` の 4 件を宣言した（`REQ-FD02` / `REQ-SEC02` / `REQ-SEC04` は
 * 既に宣言済で、横断の洗い直しで性質が 1 つ増えた。宣言済 221 → 225）。
 *
 * 語彙を足す作業と、それを使って宣言する作業は**別のコミットに分けてある**。
 * 混ぜると、上限が下がった理由が「検査を書いたから」なのか「名前を作ったから」なのか
 * 後から読めない。足した名前が要求する内容は 7 件すべてで壊して赤を確認した。
 *
 * 残した `REQ-FD04` は、形は当たるが**要件そのものを見ている検査がまだ無い**（残課題 88）。
 * ここで名乗ると「見ていないもの」に名前が付く。`REQ-TS01` / `REQ-TS03` も同じ理由で残す。
 * **検査を書く前に名前だけ足すのは、数を下げるための語彙である。**
 *
 * 2026-08-19: 16 → 14。テスト戦略（TS）の残り 6 件を点検し、**2 件**を宣言した。
 *
 *   - `REQ-TS09`（契約検査）は、前の回に足した `has-code-placement-rule` が
 *     そのまま当たる。「`contract` 単独の性質が無いから宣言できない」と
 *     書いてあったが、**この要件が見ているのは構造の境界**であって、
 *     語彙を足した時点で解けていた。名乗る先は
 *     `dependency-direction.test.ts` と `single-definition.test.ts`。
 *   - `REQ-TS01`（テストの土台を 1 箇所に集める）は、**検査を書いてから**宣言した。
 *     判定欄が指していた検査は存在しなかった（`REQ-W03` と同じ型）。
 *     `tests/architecture/test-foundation.test.ts` を新しく書いている。
 *     書いた結果、**基準時刻 `NOW` を自前で書き写したテストが 3 件**見つかった
 *     （`NOW` を動かしてもそこだけ古いまま緑で残る形）。土台へ寄せてから宣言した。
 *
 * 残した 4 件（`TS02` / `TS03` / `TS06` / `TS10`）の理由は
 * `docs/product/required-test-types.md` §4 に 1 件ずつ書いた。
 * `TS02` / `TS03` は「その層にテストがあること」を言うメタ要件で、性質を持つのは
 * 個々の業務要件の側。`TS10` は `mutation` が構造上満たしようがない。
 *
 * 2026-08-19: 14 → 13。`REQ-TS06` を宣言した。
 * **語彙のほうを足して解いた 2 度目**（`has-color-scheme-variants` → `a11y`）。
 * 横断の洗い直しで `REQ-TH01` / `REQ-TH02` にも当たり、宣言済の 2 件は性質が 1 つ増えた。
 * 足した名前が要求する内容は 3 件とも壊して赤を確認している。
 *
 * 2026-08-19: 13 → 10。残り 13 件を群に分けず 1 周し、**3 件**を宣言した
 * （`REQ-W06` / `REQ-W08` / `REQ-W11`。文章仕様の 3 つの一覧）。
 * **語彙は 1 つも足していない。**`has-enumerated-input` は既にあり、
 * 足りなかったのは検査のほうだった（`tests/domain/writing-style-tables.test.ts`、33 件）。
 *
 * 一巡で決まりを 1 つ増やした——**語彙を足した回・検査を書いた回は、
 * 未宣言の理由を全件読み直す。**`REQ-TS09` で「できない理由のほうが古くなる」形を
 * 踏んだためで、手順は `docs/product/required-test-types.md` §4 に置いた。
 * この回はその決まりで 10 件すべての理由を読み直し、**古くなっていたものは 0 件**だった
 * （`REQ-E16` は作る関数がまだ無く、`REQ-IM13` は表がまだ無く、
 * `REQ-CI12` は `scripts/verify.mjs` がいまも時間を受け取らない。3 件とも実物で確認した）。
 *
 * 壊し方は**要件の文がそのまま禁じている行為**で書いた（判定式は 1 つも触っていない）。
 * 6 通りとも赤。**うち 5 通りは、それまで緑で通っていた。**
 *
 * 2026-08-19: 10 → 9。`REQ-IM13`（改善ループの保存先）を宣言した。
 * 前の回に「表がまだ無い」と書いて残した 1 件で、**印が抜けていたのではなく
 * 当てどころが無かった**。だから減らすためにやったのは表と保存の道筋を作ることで、
 * 同じ効き方はこれで 3 度目になる（`REQ-TS01` / `REQ-FD05` / `REQ-IM13`）。
 *
 * 壊し方はこの回も要件の文が禁じている行為で、禁止のほうを取り払って
 * **禁止行為が本当に成立する状態**にして測った。22 通り測り 21 通り赤。
 * 1 周目は 17 通りしか赤にならず、**5 通りは書いたつもりで書けていなかった**
 * （4 つは検査を足して赤にした。残る 1 つは測れない形で、
 * 測れなくしている前提のほうを見張ることにした。§4 に理由がある）。
 *
 * 2026-08-19: 9 → 8。`REQ-E16`（ProductVariant）を宣言した。
 * これも印が抜けていたのではなく**当てどころが無かった**側で、同じ効き方は
 * 4 度目になる（`REQ-TS01` / `REQ-FD05` / `REQ-IM13` / `REQ-E16`）。
 * ただし前 3 件と違い、E16 は**型だけが在って誰も組み立てていなかった**
 * （`src` と `tests` を通して参照 0 件。追跡表の「見本データ」も事実ではなかった）。
 * 作ったのは組み立てる関数と断り 4 か所で、画面は作っていない。
 * 4 か所を 1 か所ずつ通る側へ変えて全件走らせ、**4 通り測って 4 通り赤**。
 *
 * 2026-08-19: 8 → 7。`REQ-CI12`（目標時間の超過は警告であって落とさない）を宣言した。
 * これも当てどころが無かった側だが、前 4 件と違うのは**当てどころが「無かった」のではなく
 * 「通らなかった」**ことである。判定そのものは前からここに書いてあった。ただし実測 46 秒に
 * 対して目標 5〜20 分なので、超過の道は現実には一度も通らない。
 * **通らない道は、そこに何が書いてあっても誰も確かめられない。**
 *
 * 減らすためにやったのは、判定を 4 つの純粋な関数へ出し
 * （`judgeBudget` / `readElapsedOverride` / `describeOverride` / `judgeRun`）、
 * 時間だけを外から渡せるようにすることだった。渡したことは必ず画面に出す
 * ——出さない口を作ると、測定のための入口がそのまま「遅いのに速いふりをする」入口になる。
 *
 * 壊し方は 4 通りとも実装側で、要件の文がそのまま禁じている行為にした
 * （時間を exit code に混ぜる／超過を blocking にする／渡したことを黙る／境目を 1 秒ずらす）。
 * 4 通りとも赤。実物でも `VERIFY_ELAPSED_SECONDS=99999 pnpm run verify --tier 1` を走らせ、
 * 警告が出たうえで exit 0 になることを確かめてある。
 */
export const TEST_TYPES_MAX_UNDECLARED = 7;

/**
 * 理由つき除外を許す上限（件数）。
 *
 * 除外は理由を書けば通る。**それだけだと、全部を除外にすれば緑になる。**
 * だから理由の質（人が読む）と件数（機械が数える）の両方で縛る。
 *
 * 2026-08-17 の実測は **11 件**。内訳は `docs/product/required-test-types-report.md`。
 * すべて「対象が実在しない」（端の無い入力に境界値、スタブしかない外部に障害注入）である。
 *
 * 経緯: 13 → **11**。内訳が変わっている。
 * キーボード操作の除外 7 件は `tests/ui/keyboard-operation.test.tsx` を書いて**消した**。
 * 代わりに、要件を 23 件足したことで新しい除外が 5 件増えた（差し引き 2 件減）。
 *
 * 経緯: 11 → **7**（2026-08-18、`ah-wes` / `ah-44d`）。
 * 語彙に `has-enumerated-input`（等価分割＋判定表）を足したことで、
 * 「入力が列挙なので `boundary` は書けない」という理由の除外 3 件
 * （`REQ-SEC06` `REQ-SEC07` `REQ-P02`）が、除外ではなく**宣言**で表せるようになった。
 * 除外を 1 件も使わずに `REQ-WC01` `REQ-WC03` `REQ-WC05` `REQ-WC07` `REQ-WC08` の
 * 5 件を足せたのも同じ理由による。
 *
 * **上げて緑にすることを禁じる。** 要件を足すと除外も増えやすいので、
 * 増やしたくなったら**先に足りない検査を書く**（今回はそうした）。
 * 空きが無くなったときの道は 1 つだけで、既存の除外を検査で消すことである。
 */
export const TEST_TYPES_MAX_EXCLUSIONS = 7;

/*
 * --- 語彙そのものを見る 3 つ（2026-08-19） -------------------------------
 *
 * 上の 2 つは「要件がどれだけ宣言できていないか」を見ている。
 * こちらが見るのは**語彙の側**である。`TEST_TYPES` に載っているのに
 * `REQUIRED_TEST_TYPES` のどこからも指されていない語は、一度も要求されない。
 * **名前があるだけで、門としては存在していない。**しかも一覧を眺めた人には
 * 全部使われているように見える（残課題 78 ⑯。`regression` が実例で、
 * 一覧に最初から載っていながら 2026-08-19 まで指し手が 0 だった）。
 *
 * **向きが逆の 2 つを対にしてある。片方だけだと抜けられる。**
 *
 * 指されていない語の数（上限）だけを張ると、**その語を一覧から消せば下がる**。
 * 消える候補は `visual` `e2e` `perf` `load` `injection` `csrf` `rate-limit` で、
 * どれも**仕様が要求しているが、まだ書けていない検査の名前**である。
 * 特に `visual` は `ah-h57`（見た目の崩れを機械が見つける手段が無い）が
 * 追っている穴そのもので、**消すと穴に名前が無くなり、穴が消えたように見える**。
 * それは 78 ⑨（対象の側が空）を、この設定ファイル自身の手で作ることになる。
 *
 * そこで語数に**下限**を張り、上げる方向にしか動かさないことにした。
 * この 2 つが揃うと、上限を下げる道が「**実際に指す**」だけになる。
 * 消して下げる道は下限に当たって塞がる。
 *
 * 片方だけを見た人が「向きを揃えよう」と言い出したときの答えがこれである。
 * **揃えると抜け道が開く。**逆向きであることが仕掛けの本体である。
 *
 * 数字はいずれも 2026-08-19 の実測そのままで、余裕を持たせていない。
 * 余裕は「まだ動かせる幅」に見え、動かしてよい幅として使われる。
 */

/**
 * どの性質からも指されていない種別の数（上限）。**下げる方向にしか動かさない。**
 *
 * 下げてよいのは、その語を**実際に指したとき**だけである。
 * 一覧から消して下げるのは、次の `TEST_TYPES_MIN_VOCABULARY` が止める。
 */
export const TEST_TYPES_MAX_UNPOINTED = 15;

/**
 * 種別の語彙の数（下限）。**上げる方向にしか動かさない。**
 *
 * 語を消すと「指されていない数」が下がるので、消す道をここで塞ぐ。
 * 書けていない検査の名前は、書けるようになるまで一覧に置いておく。
 * **名前が無い穴は、穴として数えられない。**
 */
export const TEST_TYPES_MIN_VOCABULARY = 35;

/**
 * 一覧に無いのに指されている語の数。**0 で固定**（上げも下げもしない）。
 *
 * ここが 0 でなくなるのは打ち間違いのときだけで、そのとき要求される種別は
 * どのテストからも名乗りようが無い。**要求はされるが満たしようが無い**状態は、
 * 除外理由の欄を埋めるだけで終わり、門としては何も見ていない。
 */
export const TEST_TYPES_MAX_STRAY = 0;

/**
 * --- 0 を主張する検査に、母集団の床が同居しているか（対で見る）---
 *
 * ② の形（塞げていない穴を「いま 0 件である」と固定し、塞がった日に赤くなる）は、
 * **0 が 2 通りの理由で出る**——悪さが消えたときと、**数える対象そのものが消えたとき**。
 * 後者では検査は緑のまま黙る。だから ② には母集団の件数の床を同居させる。
 *
 * ここも `TEST_TYPES_MAX_UNPOINTED` / `TEST_TYPES_MIN_VOCABULARY` と同じ対である。
 * **片方だけでは抜けられる**——床を足す代わりに検査そのものを消せば、上限は下がる。
 * 族の下限がその道を塞ぐ。**揃えると抜け道が開く。逆向きであることが仕掛けの本体である。**
 *
 * 数える単位は `it` 1 つ。族に入る条件は 2 つで、(1) 「0 件である」と主張していること
 * (2) その 0 の母集団が**実行時にテストの外から集められている**こと。
 * テスト内のリテラル配列は族の外に置く——消せば同じファイル上に見えるので、
 * 黙って消える形にならない（2026-08-19 時点で 266 件がこちら側）。
 *
 * **数える条件を 1 度動かした。動かす前の条件とそのときの数も残す**（残課題 78 ㉗）。
 *   - 前: 「0 件である」の判別を `toStrictEqual\(\[\]\)` と 1 行で書いていた
 *     → 族 32 / 床なし 25 / 族外 262
 *   - 後: 整形で `toStrictEqual(\n  [],\n)` へ折れた形と末尾カンマを含めた
 *     → 族 34 / 床なし 27 / 族外 266（うち 2 件へ床を足して 25 へ戻した）
 *   前の条件は**行を単位に測っていた**ため、整形が改行を入れた `it` を族から落としていた。
 *   ㉓ そのものであり、下限の側で 1 度やった同じ誤りを上限の側で繰り返している。
 *
 * **測り方の限界を先に書いておく（数字より重要）。**この数え方は床を
 * 「`.length` に対する `toBeGreaterThan` 系」で見つけるため、**別の形の床が見えない。**
 * 実例が 1 件ある: `tests/architecture/guard-inline-python-hole.test.ts` の
 * 「見張りが生きていること」は、止まる例を同居させる形（`toBe(2)`）で床を張っており、
 * 機械には見えていない。逆に `tests/ui/copy-dictionary.test.ts:125` は床を持つと
 * 判定されるが、張り先が走査対象ではなく辞書のほうである。
 * **この 2 つの誤りは逆向きで、いまはたまたま相殺している。**数が合っているのは
 * 測れているからではない。**床は「あるか」ではなく「何に対して張ってあるか」まで見ないと数えられない。**
 * これは残課題 78 ㉙ の初出である——**誤りが逆向きに 2 つあると、合った数が出る。**
 * 相殺していると言えるのは、**片方だけ直すと数が動くことを確かめたとき**だけである。
 * 確かめた（2026-08-19、族 34 / 床なし 25 の状態で再確認）: (a) だけ直すと 25 → 24、
 * (b) だけ直すと 25 → 26。**直すときは 2 つ同時に。**片側だけ直すと上限に当たる。
 *
 * **↑ここまでが「数え方を直す」方針での見積もりである。実際には方針を変えて直した**
 * （2026-08-19、6 度目）。数え方（検出側）を直そうとすると、(b) の「辞書」と「走査対象」を
 * 区別する手がかりが変数名しかなく、当てにならない見分け方を検出側へ入れることになる。
 * (a) の `toBe(2)` を床として認めると、**0 以外の等値をすべて床と認める**ことになり、
 * これは測る側を緩めることそのものである。そこで**検出側ではなく検査 2 本の側を直した。**
 *   - (a) `guard-inline-python-hole.test.ts` … 見張りの行数の床（> 300 行）を足した。
 *     止まる例（`toBe(2)`）はそのまま残してある。**床を 1 つ足しただけで、何も緩めていない。**
 *   - (b) `copy-dictionary.test.ts:125` … 走査対象そのもの（`screenFiles` / `banned`）へ
 *     床を足した。**この検査は元から床ありと数えられていたので、数は動かない。**
 *     動いたのは「床ありという判定が正しくなった」ことのほうである。
 * 結果は **族 47 / 床なし 24 / 族外 253**。上限は 25 → **24 へ下げた**（下げる向き）。
 *
 * **足した 2 つの床は、壊して赤を見た**（複製を scratchpad へ取り、書き戻しは複製から）:
 * 見張りを 4 行のスタブへ置き換える → (a) が `expected 4 to be greater than 300` で赤。
 * 辞書の「## 使ってはいけない言い換え」の見出しを変える → (b) が `expected 0 to be greater than 5` で赤。
 * **ただし (a) について正直に書いておく: この壊し方は、床が無くても同じ `it` の中の
 * `alive.code === 2` が拾う。**つまり (a) の床は**新しい検出を足したのではなく、
 * 既にあった検出を機械から見える形にした**ものである。数が動いたのはそのためで、
 * 見張りの強さは変わっていない。
 * **(b) の見積もり「25 → 26」は、この方針では起きない。**方針が変わったので当たらない、
 * という意味であって、見積もりが正しかったわけではない。
 *
 * ここから 1 つ残る: **床の有無は数えられているが、床が母集団に掛かっているかは
 * 数えられていない。**(b) はその一例で、直したのは検査であって数え方ではない。
 * つまり同じ形（辞書にだけ床がある）は、いまも見つけられない。
 *
 * **族の側の過小計上は直した**（2026-08-19、5 度目の条件変更）。**前: 族 34 / 床なし 25 /
 * 族外 266。後: 族 47 / 床なし 37 / 族外 253。**増えた 12 件すべてに床を足して床なしは 25 へ戻し、
 * **上限 25 は上げていない。理由つき除外も 1 件も増やしていない。**下限は 34 → 47。
 *
 * 直した中身は 2 つで、どちらも**過小に出る向きの誤り**だった。
 *   (1) 母集団を集める処理が**関数へ切り出されている**形を拾えていなかった
 *       （`const ALL = testFiles(TESTS).map(...)`）。走査を含む関数の名前を先に集め、
 *       その関数を呼んでいる変数も母集団と見なす 2 段にした。
 *       **集める処理は関数に切り出されるのが普通なので、直書きだけを見る数え方は構造的に過小になる。**
 *   (2) 変数の初期化式を `([\s\S]{0,300})` と**捕捉**で書いたため `lastIndex` が 300 字進み、
 *       その中にある次の `const` を丸ごと飛ばしていた。先読みへ直した。
 *       **この誤りは 1 度、途中の実測（床なし 29 件）として出ている。**
 */

/**
 * 母集団の件数の床を持たない ② 形の検査の数（上限）。**下げる方向にしか動かさない。**
 *
 * 下げてよいのは、その検査に**実際に床を足したとき**だけである。
 * 検査ごと消して下げるのは、次の `FORM2_MIN_FAMILY` が止める。
 */
export const FORM2_MAX_WITHOUT_FLOOR = 24;

/**
 * 族そのものの件数（下限）。**上げる方向にしか動かさない。**
 *
 * 0 を主張する検査を消すと上限は下がるので、消す道をここで塞ぐ。
 * **穴を見張っている検査を減らして数字を良くする道**が、いちばん通りやすい抜け方である。
 */
export const FORM2_MIN_FAMILY = 47;

/**
 * スタブと見なす場所。
 *
 * **カバレッジ計算から除外しない。** 除外すると、除外の線引きを動かすだけで
 * 数字を作れてしまう。ここは「全体」と「スタブを除いた実質」を**併記**するための
 * 目印であって、除外リストではない。
 *
 * 判断: docs/spec/10-テスト戦略仕様.md §2-1
 */
export const STUB_PATTERNS = [
  "src/infrastructure/persistence/sample/",
  "-sample-repository.ts",
  "-sample-sink.ts",
  "sample.ts",
];

/**
 * 実質カバレッジと全体カバレッジの差の上限（ポイント）。
 *
 * スタブは「呼ばれたら必ず失敗を返す」だけの短い関数なので、
 * テストが通りやすく分母も小さい。放っておくと、ここを厚くするだけで
 * 全体の数字が上がっていく。**その差の広がりが数字合わせの兆候**である。
 *
 * 起点は +11.55pt（実質 51.92% / スタブ 63.47%）で、
 * 画面の総当たり描画を入れた時点で +0.7pt まで縮んだ。
 * 差が広がる方向へ動いたら、テストを足す場所を間違えている。
 */
export const MAX_STUB_GAP_POINTS = 3;

/**
 * スタブと実質の差の判定。**片側だけを見る。**
 *
 * 見張りたいのは「スタブを厚くして全体の数字を作る」という 1 方向だけである。
 * 逆向き（実質がスタブを上回る）は本物のコードの方が厚く検査されている状態で、
 * これは求めている姿そのものなので、止める理由が無い。
 *
 * 絶対値で見ると、望ましい方向へ大きく進んだときにも警告が出る。
 * 「良くなったのに赤くなる」検査は、そのうち誰も読まなくなる。
 *
 * @param {number} realLines 実質（スタブを除いた）行カバレッジ
 * @param {number} stubLines スタブのみの行カバレッジ
 * @returns {{ gap: number, exceeded: boolean, note: string }}
 */
export function judgeStubGap(realLines, stubLines) {
  const gap = Math.round((stubLines - realLines) * 100) / 100;
  const exceeded = gap > MAX_STUB_GAP_POINTS;
  const note = exceeded
    ? `スタブが実質より ${gap}pt 高い（上限 ${MAX_STUB_GAP_POINTS}pt）。テストを足す場所が本物のコードではなくスタブに寄っています`
    : gap > 0
      ? `上限 ${MAX_STUB_GAP_POINTS}pt 以内`
      : "スタブは実質を上回っていない（望ましい向き）";
  return { gap, exceeded, note };
}

/**
 * 検査の段（1 / 2 / 3）。
 *
 * **重いテストを足せる置き場所を、重いテストより先に作る。**
 * 順序を逆にすると、CI が回らなくなってから「重いテストを消す」判断に流れる。
 * ここでやることは 1 つだけ、**実行する場所を変える**ことである。
 * テストを消す・skip する・閾値を下げるのは、いずれもこの設計の目的に反する。
 *
 * 費用の前提（2026-08-17 実測）:
 *   - このリポジトリは **public**。GitHub Actions の標準ランナーは無料・無制限で、
 *     直近の実行は 40〜70 秒。**実行時間は費用の要因ではない**。
 *   - 課金されるのは **AI の評価セット**（`AI_EVAL_BUDGET`）だけである。
 * したがって「時間を減らすために CI からテストを外す」判断はしない。
 * 単体テストは CI に置き続ける（手元で流し忘れた場合と、手元だけで通る場合を捕まえる）。
 *
 * `runOn` は実行場所で、**ここ 1 か所で切り替える**。
 *   - `ci`     機械の上で毎回走る
 *   - `local`  手元でだけ走らせる（将来このリポジトリを非公開にしたときの逃がし先）
 *   - `manual` 人が起動したときだけ走る
 * テストファイル側が持つのは**段の印だけ**で、実行場所は書かない。
 * 書くと、場所を変えるたびに全テストを触ることになり、
 * そのとき人は「移す」より「消す」を選ぶ。
 *
 * `targetMinutes` は**目標であって、落とす条件ではない**。
 * 超えたら警告を出す。時間で赤くすると、時間を守るためにテストを削る力が働く。
 */
export const TIERS = [
  {
    id: 1,
    key: "fast",
    label: "速い門",
    runOn: "ci",
    triggers: ["push", "pull_request"],
    targetMinutes: 5,
    blocksMerge: true,
    contains: "型検査 / 書き方 / 段の指定漏れ / 単体・契約検査",
    why: "書いている最中に返ってくる速さを守る。ここが遅くなると、人は push をためらう",
  },
  {
    id: 2,
    key: "wide",
    label: "広い門",
    runOn: "ci",
    triggers: ["pull_request"],
    targetMinutes: 15,
    blocksMerge: true,
    contains:
      "結合 / API 契約 / 画面 / 読み上げ / 境界値 / カバレッジ閾値 / 変更範囲だけのミューテーション",
    why: "マージの直前に 1 回でよいもの。ここを毎 push に混ぜると 1 段の速さが死ぬ",
  },
  {
    id: 3,
    key: "deep",
    label: "深い門",
    runOn: "manual",
    triggers: ["schedule", "workflow_dispatch"],
    targetMinutes: 90,
    blocksMerge: false,
    contains: "全体ミューテーション / 負荷 / 見た目の回帰 / 脆弱性の深掘り / AI 評価セット",
    why: "落ちてもマージは止めない。止めると、夜間の赤を消すために夜間の検査が外される",
  },
];

/** 段の番号の一覧。テストに付けた印の検証に使う。 */
export const TIER_IDS = TIERS.map((t) => t.id);

/**
 * AI 評価セットの費用の上限。**この 2 つだけが従量課金の対象**である。
 *
 * `workflow_dispatch`（手動起動）のみとし、定期実行にも PR にも載せない。
 * 定期実行に載せた瞬間、誰も見ていない時間に費用が発生し続ける。
 *
 * 上限は**途中で止まる**ように使う（走り終えてから「超えました」と言わない）。
 * 見積り費用と実費用の両方を出し、`docs/product/ai-eval-cost.md` に
 * カバレッジと同じやり方で記録する（手で書き換えない）。
 *
 * 非 AI の構造検査（`tests/evals/generation-eval-set.test.ts`）はここに含めない。
 * あれは提供元へ 1 回も問い合わせないので、毎回の CI で走らせ続ける。
 */
export const AI_EVAL_BUDGET = {
  maxCases: 51,
  maxTokens: 400_000,
  why: "評価セットは 51 件。1 件あたり入出力で 8,000 トークンを上限の目安とした",
};

/**
 * 検査の並び。`scripts/verify.mjs` はこの順にそのまま実行する。
 *
 * 順番には理由がある。**安いものから先に落とす**。
 * 型が合っていないコードのテストを 30 秒かけて走らせても、分かるのは同じことである。
 * この並びは CI 側でも崩さない（`ci.yml` は段を呼ぶだけで、中身を持たない）。
 *
 * `blocking: false` は「落ちても止めないが、必ず出力する」もの。
 * 依存の脆弱性は上流の更新待ちで手が止まるため、警告どまりにする。
 *
 * `tier` はその検査が属する段。`node scripts/verify.mjs --tier 1` のように選べる。
 * 指定しなければ 1 段と 2 段を続けて走らせる（＝これまでと同じ内容）。
 */
export const CHECKS = [
  {
    id: "typecheck",
    label: "型検査",
    command: ["pnpm", "run", "typecheck"],
    blocking: true,
    tier: 1,
    why: "最も速く、最も多くの壊れ方を捕まえる。vitest は型を見ないので別に必要",
    /*
     * **`--incremental false` を外さないこと。**
     *
     * 2026-08-18 に実測した。この門は緑のまま、`pnpm run preview`（本番の
     * 組み立て）だけが型の誤りで落ちていた。前回の結果が焼き付いていて、
     * **変わったファイルを見直していなかった**。
     *
     * 門が緑で本番が落ちるのは、門が無いのと同じではなく**それより悪い**。
     * 無ければ preview で確かめるが、緑があると確かめずに進む。
     * 速さのために毎回見直しをやめると、やめたことは緑として現れる。
     */
  },
  {
    id: "lint",
    label: "書き方の検査",
    command: ["pnpm", "run", "lint"],
    blocking: true,
    tier: 1,
    why: "層をまたぐ import と色の直書きを、編集中に気づける場所で止める",
  },
  {
    id: "tier-audit",
    label: "段の指定漏れ",
    command: ["node", "scripts/tier-audit.mjs"],
    blocking: true,
    tier: 1,
    why: "段の印が無いテストは、どの段でも走らないまま緑になる。テストを走らせる前に潰す",
  },
  {
    id: "migration-generated",
    label: "マイグレーションの作り忘れ",
    command: ["node", "scripts/migration-generated.mjs"],
    blocking: true,
    tier: 1,
    why: "スキーマだけ変えて公開すると、存在しない列を読んで本番が落ちる。1 秒で終わるので手元でも走らせる",
  },
  {
    id: "test",
    label: "テストとカバレッジ",
    command: ["node", "scripts/run-tests.mjs", "--coverage"],
    blocking: true,
    tier: 1,
    why: "単体・結合・画面・契約検査はすべてここで走る。閾値未達もここで落ちる",
  },
  {
    id: "mutation-changed",
    label: "変更したところのミューテーション",
    command: ["node", "scripts/mutation.mjs", "--changed"],
    blocking: true,
    tier: 2,
    why: "カバレッジは「通ったか」しか見ない。**書き換えても気づかないテスト**をここで見つける",
  },
  {
    id: "coverage-report",
    label: "層別の記録",
    command: ["node", "scripts/coverage-report.mjs"],
    blocking: true,
    tier: 2,
    why: "層別の下限と、スタブとの差を見る。全体 80% だけでは薄い場所が隠れる",
  },
  {
    id: "traceability",
    label: "テストと要件の対応",
    command: ["node", "scripts/traceability.mjs"],
    blocking: true,
    tier: 2,
    why: "**どの要件のために書いたのか辿れないテスト**が増えていないか見る。由来の無いテストは実装をなぞっているだけで、実装が間違っていても緑になる",
  },
  {
    id: "required-test-types",
    label: "要件ごとの必須テスト種別",
    command: ["node", "scripts/required-test-types.mjs"],
    blocking: true,
    tier: 2,
    why: "種別を決めないと、テストは**書きやすいところから書かれる**。権限のできてはいけない側・禁止された遷移・障害注入が永久に残るのを止める",
  },
  {
    id: "port-wiring",
    label: "つなぎ目の呼び出し",
    command: ["node", "scripts/port-wiring.mjs"],
    blocking: true,
    tier: 2,
    why: "**口はあるが誰も呼んでいない**を見る。表現ポリシー・読者ページの道具・操作の記録で 3 回続いた形で、3 つとも画面上は正常に見えるため使っても気づけない。接続を渡しているかを見る検査（composition-wiring）はこれを通り抜けた",
  },
  {
    id: "spec-freshness",
    label: "仕様レポートの鮮度",
    command: ["node", "scripts/spec-freshness.mjs"],
    blocking: true,
    tier: 2,
    why: "評価後に仕様書を書き換えると、古い PASS が古く見えないまま残る。指紋で気づける形にする。2026-08-19 まで警告どまりで、しかも判定の答えを捨てて必ず 0 を返していたため、指紋が無いまま緑を出し続けていた（この作業でいちばん大きい主張『仕様を全て満たした』の根拠がそれである）",
  },
  {
    id: "audit",
    label: "依存の脆弱性",
    command: ["pnpm", "audit", "--audit-level", "high"],
    blocking: false,
    tier: 2,
    why: "数秒で済む。ただし上流待ちで作業が止まるのを避けるため警告どまり",
  },
  {
    id: "mutation",
    label: "全体のミューテーション",
    command: ["node", "scripts/mutation.mjs"],
    blocking: true,
    tier: 3,
    why: "domain + application の全体。CI 相当の並列度で 27 分（実測 2026-08-17）かかるので 2 段には置けない。夜間に測って推移を見る",
  },
];

/**
 * `--tier` の指定から、走らせる検査を選ぶ。
 *
 * 指定が無いときは `runOn: "ci"` の段だけを走らせる。
 * 3 段は夜間・手動のもので、手元で `pnpm verify` を打った人を
 * 27 分待たせる意味が無い。
 *
 * @param {number[] | null} tiers 走らせたい段。空か null なら既定
 * @returns {typeof CHECKS}
 */
export function checksForTiers(tiers) {
  const wanted =
    tiers === null || tiers === undefined || tiers.length === 0
      ? TIERS.filter((t) => t.runOn === "ci").map((t) => t.id)
      : tiers;
  return CHECKS.filter((c) => wanted.includes(c.tier));
}

/**
 * 公開できる条件（リリースゲート）。
 *
 * **この 2 つ以外の理由で公開を止めない。** 他の指摘は残課題リストへ回す。
 * ゲートを増やすほど、ゲートを無視する運用に近づく。
 */
export const RELEASE_GATES = [
  { id: "verify", label: "`pnpm verify` が通っている" },
  { id: "critical-zero", label: "セキュリティ監査の CRITICAL が 0 件" },
];

const qualityGates = {
  GLOBAL_COVERAGE,
  LAYER_COVERAGE,
  MUTATION_SCORE,
  MUTATION_MAX_CHANGED_FILES,
  TRACEABILITY_MAX_UNLINKED,
  STUB_PATTERNS,
  MAX_STUB_GAP_POINTS,
  judgeStubGap,
  TIERS,
  TIER_IDS,
  AI_EVAL_BUDGET,
  checksForTiers,
  CHECKS,
  RELEASE_GATES,
};

export default qualityGates;

/**
 * つなぎ目（ポート）の手続きのうち、**製品コードから呼ばれていない**ものの上限。
 *
 * この案件では「口はあるが誰も呼んでいない」が 3 回続いた
 * （表現ポリシー・読者ページの道具・操作の記録）。3 回続いたら偶然ではない。
 * 1 件ずつ見つけて潰す限り、見つかっていないものは残り続ける。
 *
 * 数えるのは `src/application` `src/presentation` `src/app` からの呼び出しだけで、
 * **テストからの呼び出しは数えない**。数えると全部緑になり、
 * この検査自体が 4 件目の穴になる。
 *
 * ポート単位ではなく**手続き単位**で見る。ポート単位で「1 つでも呼ばれていれば OK」
 * にすると、操作の記録のような**一部だけ埋まった状態**を見逃す。
 * 実際、記録は承認と段階の移動しか書いておらず、公開は記録されていない。
 *
 * 2026-08-18 の実測は **82 件 / 176 件**（ポート 56 件）。
 * この数字は「呼ばれていない口が 82 件ある」という事実の記録であって、目標ではない。
 * 大半は保存先や機能そのものがまだ無いもので、機能が乗るたびに減る。
 *
 * **上げて緑にすることを禁じる。** ポートを足すときは、
 * 呼ぶ場所を同じ変更の中で作る。作れないなら理由を登録簿へ書く。
 */
export const PORT_WIRING_MAX_UNCALLED = 82;

/**
 * 理由つきの除外の上限。
 *
 * 「意図的に呼んでいない」ものは `docs/product/port-wiring.md` に
 * **理由を書いて**登録する。理由が空の行は除外として認めない。
 * 除外にも上限を置くのは、上限に当たったときの逃げ道を塞ぐためである。
 *
 * 2026-08-18 の実測は **0 件**。まだ 1 件も除外していない。
 */
export const PORT_WIRING_MAX_EXCLUSIONS = 0;

/**
 * **書き込みなのに操作の記録へ届いていない入口**の上限。
 *
 * 上の `PORT_WIRING_MAX_UNCALLED` は手続き単位で「1 回でも呼ばれたか」を見る。
 * それでは足りないことが分かったので、こちらを足した。
 *
 * 操作の記録は**呼ばれている**（`AuditLogPort.append` は上の総ざらいで緑）。
 * にもかかわらず、2026-08-18 の実測では
 * **書き込みの入口 21 件のうち、記録へ届いているのは 1 件だけ**だった
 * （`manage-content.ts` の承認だけ。公開も配信も成果の調整も記録されていない）。
 * 「口が塞がっている」ではなく「**一部の経路からしか通っていない**」形で、
 * 手続き単位の総ざらいは原理的にこれを拾えない。
 *
 * この形の穴は、この案件で 4 回とも**別の作業のついでに偶然**見つかっている。
 * ここを固定する目的は、塞ぐことではなく**偶然に頼るのをやめる**ことである。
 *
 * 20 件は事実の記録であって目標ではない。読者の操作のように
 * 記録が要らないものも混ざっているので、精査した分は理由つきの除外へ移す
 * （そのぶん、この数は下がる）。
 *
 * 2026-08-18 に 20 件を精査し、**読者の操作 3 件と自動の計測 1 件**を
 * 理由つきの除外へ移した（`docs/product/port-wiring.md`）。20 → 16。
 * 除外しなかったものの判断も同じ場所に書いてある
 * （改善要望 3 件・下書きの保存 2 件）。**除外を増やして下げたのではなく、
 * 記録の要らないものを分けただけ**なので、残る 16 件は全部塞ぐ対象である。
 *
 * 同日、取り返しがつかない側から順に記録を足して 16 → 11。
 * 塞いだのは公開（`createPublishArticleUseCase`）、配信の予約・取り消し・
 * 予定日の変更（3 経路）、取得用の鍵の発行と失効。
 * 残る 11 件は、改善要望 3・受信箱 4・下書き 3・成果の手修正 1。
 *
 * 続けて、取り返しがつかない残り 2 件を塞いで 11 → 9。
 * ブログを作る（`createCreateSiteFromDraftUseCase`。消す口が無い）と、
 * 成果の金額の手修正（`createAdjustConversionUseCase`。締めの報告に使われる）。
 * 残る 9 件は、改善要望 3・受信箱 4・下書き 2。
 * どれも**間違えたら直せる**もので、取り返しのつかない側は無くなった。
 *
 * 続けて受信箱の 4 経路を塞いで 9 → 5（受け取り・広告主を決める・
 * 商品に結びつける・対象外にする）。**対象外にするだけは出来事を流していない**ので、
 * 記録が無いと捨てた操作の跡が 1 つも残らない状態だった。
 * 残る 5 件は、改善要望 3・下書き 2。
 *
 * 続けて改善要望の 3 経路を塞いで 5 → 2（届いた・扱いを変えた・外へ払い出した）。
 * **払い出しは中身がこちらの手を離れる唯一の口**で、記録が無いと
 * 「どの要望が・いつ・どの経路で外へ出たか」を後から言えなかった。
 * 残る 2 件は下書きの 2 入口（始める・段を保存する）。
 *
 * 最後に下書きの 2 入口を塞いで 2 → 0。ここだけは**記録が残せなくても
 * 操作を止めない**（下書きは読者に見えず、何度でも上書きでき、捨ててよい）。
 * 止めた版では、記録の置き場が無い段でウィザードが 1 段目から進まなくなった。
 * この検査が見るのは「入口から記録へ辿れるか」なので、この形でも数は 0 になる。
 * 止める／止めないの別は各ユースケースのテストが固定している。
 *
 * **上げて緑にすることを禁じる。**
 */
export const PORT_WIRING_MAX_UNRECORDED = 0;

/**
 * 書き込み側の理由つき除外の上限。
 *
 * 「この入口は記録しなくてよい」と判断したものを
 * `docs/product/port-wiring.md` へ理由つきで登録する。
 * 精査して除外を足すときは、この数と `PORT_WIRING_MAX_UNRECORDED` を
 * **同じ変更の中で**動かす（片方だけ動かすと合計が増える）。
 *
 * 2026-08-18 に 4 件（読者の問い合わせ・気になる商品の出し入れ 2 件・
 * 読者の計測）を登録した。**除外にできる理由は 2 つだけ**に絞ってある
 * （読者の操作である / 自動の計測である）。理由の種類を増やせるようにすると、
 * この数は必ず増える。3 つ目の理由を書きたくなったら、そこが判断の分かれ目で、
 * たいていは記録を足す側が正しい。
 */
export const PORT_WIRING_MAX_WRITE_EXCLUSIONS = 4;

/**
 * **読み書きを判定できなかった手続き**の上限。
 *
 * 書き込みかどうかは手続きの名前の頭で判定する。読みの語彙と書きの語彙を
 * **両方**並べ、どちらにも当たらないものをここで数える。
 *
 * 片側だけを並べると必ず穴が空く。「読み以外は全部書き」にすると
 * `newId` `signedUrl` `current` のような動詞でない手続きが書き込みに化け、
 * 「書き以外は全部読み」にすると**並べ忘れた動詞が黙って漏れる**。
 * 漏れる側の穴は、この検査自体を 5 件目の穴にする。
 *
 * だから 3 つ目に分ける。判定できないものは赤にも緑にもせず、
 * **件数を固定して見えるようにする**だけにする。
 */
export const PORT_WIRING_MAX_UNKNOWN_VERBS = 0;

/**
 * **記録が残せなくても進む入口**の上限。
 *
 * 上の 2 つは「記録へ**届いているか**」を見ている。届いてさえいれば緑になる。
 * ここは届いた先の話で、`AuditLogPort.append` の戻り値を受けずに捨てている
 * 入口を数える。捨てると、書けなかったときに**操作は成功し、記録だけが無い**
 * 状態が残る。
 *
 * この状態は両側から見えない。押した人には成功として返り、あとで記録の一覧を
 * 開いた人には「その操作は無かった」ように見える。**食い違いに気づく機会が
 * どこにも無い**ので、届いているかを見るだけでは足りない。
 *
 * --- 0 にしない理由 ---
 * 見本モードでは記録の置き場が無く、下書きの開始と段階の保存で必ず失敗する。
 * ここで断ると**下書きが 1 段目から進まなくなる**。この 2 つは失っても
 * やり直せる（下書きは何度でも作れる）ので、意図して捨てている。
 * 公開・削除・課金にあたる操作は 1 件も捨てていない。
 *
 * だから 0 ではなく、**いまの実測の 2 で止める**。3 件目が増えたら、それは
 * 「やり直せる下書き」以外の操作で記録を諦めた可能性が高い。
 * **上げて緑にすることは禁止。下げる方向にしか動かさない。**
 */
export const PORT_WIRING_MAX_AUDIT_BEST_EFFORT = 2;

/**
 * **出す場所を持たない記録の語**の上限。
 *
 * `AuditAction` には 28 語ある。上の 3 つはどれも「入口から記録へ届いているか」を
 * 見ており、**語の側から見ていない。**だから、語が union にあるだけで
 * どこからも出されていない状態は、3 つとも緑のまま通る。
 *
 * これは実際に穴だった。`export.performed` は語だけがあって出す場所が無く、
 * 調べたら `createExportManualDraftUseCase` が**記事の本文を人へ丸ごと渡すのに
 * 記録を 1 件も残していなかった**（2026-08-18 に修正）。
 * **語だけがあって出す場所が無いのは、機能の抜けの影である。**
 *
 * 語が並んでいると、記録の一覧を読む人は「その操作は記録されている」と思う。
 * 実際には 1 行も出ない。**書いていないことは、書いていないようには見えない。**
 *
 * --- 数え方 ---
 * `tests/architecture/audit-action-emitters.test.ts` が `src/` を読んで数える。
 * 日本語ラベルの表（`"語": "日本語"`）は出す場所として数えない。
 * 手で数えた表を置かないのは、**手で書いた数字は古くなっても古く見えない**ため。
 *
 * --- 0 にしない理由 ---
 * 6 語は**機能そのものがまだ無い**（`member.role_changed` は残課題 62、
 * `connector.*` は ASP 接続がスタブ、`disclosure.changed` / `policy_rule.changed` /
 * `content.corrected` は操作の口が無い）。
 * 出す場所を先に作っても、呼ばれないコードが増えるだけである。
 *
 * **上げて緑にすることは禁止。下げる方向にしか動かさない。**
 * 語を消して下げるのも正しい（要件がその記録を求めていないなら、語を残さない）。
 */
export const AUDIT_ACTIONS_MAX_WITHOUT_EMITTER = 6;

/**
 * **見本データの中にしか無い記録の語**の上限。
 *
 * こちらはもっと分かりにくい。**画面には記録の行が並ぶのに、
 * その行を作った操作が存在しない。**見た目は動いているように見える。
 * 見本を消した時点で 0 件になり、そこで初めて機能が無いと分かる。
 *
 * いまの 2 語は `content.created`（記事を作る）と
 * `ranking_model.changed`（評価基準を変える）で、どちらも
 * **書き込みのポートはあるが、それを呼ぶ操作がどこにも無い**
 * （`variants.save` は承認の 1 か所だけ、`rankingModels.save` は 0 か所）。
 *
 * **上げて緑にすることは禁止。下げる方向にしか動かさない。**
 */
export const AUDIT_ACTIONS_MAX_SAMPLE_ONLY = 2;

/**
 * **実処理から出している記録の語**の下限。
 *
 * 上の 2 つは「増えたら落ちる」だけなので、**語ごと消せば緑になる。**
 * 消して減らすのは正しい直し方の 1 つ（要件がその記録を求めていないなら、
 * 語を残さない）だが、**出していた語まで一緒に消えたときに気づけない。**
 * だから出している側の数にも床を張る。
 *
 * ここは上限ではなく**下限**である。**下げて緑にすることは禁止。
 * 上げる方向にしか動かさない。**残る 6 語に機能が付いたら、そのぶん上げる。
 */
export const AUDIT_ACTIONS_MIN_EMITTED = 20;

/**
 * テナント分離の検査で、**他社の身元で呼んで `ok` まで到達した道具の下限**。
 *
 * ここと次の 1 つは、上限ではなく**下限**である。理由がある。
 *
 * テナント分離の検査は「他社の身元で呼んで、見本の会社の識別子が
 * 混ざっていないこと」を見る。ただし**断られた道具については何も見ていない**——
 * `result.ok` が false なら中身を確かめずに通る。
 * 断りの理由が「他社だから」でも「入力の見本が足りない」でも、同じく緑になる。
 *
 * 2026-08-18 に、この検査の対象を `readOnly` で絞るのをやめた。
 * `readOnly` は MCP の注釈のために付けた旗であって、
 * **「他社のデータに触れるか」とは別の問い**である。それまでは書き込みの道具
 * 25 個が丸ごと対象外で、**他社のデータを読めるより書き換えられるほうが重い。**
 *
 * 対象は 81 → 111 件になった。**ただし守りが 30 件ぶん強くなったわけではない。**
 * 実測すると `ok` まで到達したのは 61 → **66 件**（増えたのは 5 件だけ）。
 * 残る 45 件は他社かどうかを見る前に断られている。
 *
 * **この 66 は「確かめた件数」ではない。** 名前を 2026-08-18 に
 * `TENANT_ISOLATION_MIN_INSPECTED` から変えた。`ok` が返るのは 2 通りあり、
 * 「誰が呼んでも同じ公開情報」と「**他社なので該当が 0 件で、空のまま `ok`**」が
 * 混ざっているためである。後者は何も確かめていない。
 *
 * 例: `hand_off_feedback` は他社の身元で `ok` を返すが、中身は「0 件」である
 * （`findById(actor.workspaceId, id)` で弾かれて skip されている）。
 * **分離は効いているが、効いていることを `ok` という値は証明していない。**
 *
 * それでもこの数を下限に残すのは、**到達できる道具が減ったこと**を
 * 見張るためである（見本が壊れれば減る）。証明の数は次の 1 つが持つ。
 *
 * 2026-08-18（同日、後）に 66 → **69** へ上げた。上げた理由は 3 つで、
 * どれも「検査を通しやすくした」のではなく**動く土台を足した**ものである。
 *
 *   1. 見本の操作の記録が、必ず断る実装から**控え（メモリ）へ本当に書く**実装になった
 *      （`persistence:audit-log-memory`）。記録を伴う操作 25 件が、
 *      記録の手前で止まらなくなった
 *   2. 見本の下書き `sd_sample` を 1 本置いた（それまで空で始まっていた）
 *   3. 受信箱の見本の識別子を、実在する行（`li_received_1` ほか）へ直した
 *
 * **この数は「持ち主で通る件数」ではない。** 持ち主で `ok` になるのは同じ実測で
 * 97 件だが、こちらは他社の身元で `ok` まで行った 69 件である。
 * 差の 28 が下の「証明できた件数」で、2 つの数はここで噛み合っている。
 *
 * **下げて緑にすることは禁止。上げる方向にしか動かさない。**
 */
export const TENANT_ISOLATION_MIN_REACHED = 69;

/**
 * テナント分離の検査で、**分離が効いていることを実際に確かめられた道具の下限**。
 *
 * 数え方は「**同じ入力が、持ち主では通り、他社では断られる**」。
 * この 2 つが揃って初めて、断りの理由が「他社だから」だと言える。
 * 持ち主でも断られるなら、それは見本の入力が悪いだけで、分離の検査になっていない。
 *
 * 2026-08-18 の実測（対象 111 件）。左が持ち主、右が他社:
 *
 *   ok → ok                     69 件  ← 誰が呼んでも同じ結果。何も証明していない
 *   ok → NOT_FOUND              18 件  ← 確かめられた
 *   ok → TENANT_MISMATCH        10 件  ← 確かめられた
 *   CONFLICT                     5 件  ← 状態が合わない（見本の入力では作れない状態）
 *   NOT_IMPLEMENTED              5 件  ← スタブ
 *   VALIDATION_FAILED            4 件  ← 見本の値が足りない・enum に合っていない
 *
 * **確かめられたのは 28 件**である。上の 97 と比べると分かるが、
 * `ok` の数（97）と証明の数（28）は 3 倍以上違う。**安く動くほうを下限にしない。**
 * 対象を増やすのは 1 行でできるが、証明を増やすには見本を作る作業が要る。
 *
 * 20 → 28 へ上げた。**上がったぶんだけ上げている**（実測が 28 なので 28）。
 * 内訳では `ok → ok` の 69 件が最も多い。ここは分離が効いていないのではなく、
 * **効いていることをこの数え方では言えない**だけである
 * （公開情報を返す道具と、他社では 0 件になって空で `ok` になる道具が混ざる）。
 * ここを分けるには、`ok` の中身が他社では空であることまで見る必要がある。残課題。
 *
 * 残る 14 件（CONFLICT 5 / NOT_IMPLEMENTED 5 / VALIDATION_FAILED 4）は
 * 入力の見本を直しても増えない。状態を作るか、実装を足すかのどちらかが要る。
 *
 * **下げて緑にすることは禁止。上げる方向にしか動かさない。**
 */
export const TENANT_ISOLATION_MIN_SEPARATION_PROVEN = 28;

/**
 * **意図より広く開いている入口**の上限（`docs/product/open-doors.md`）。
 *
 * 「ログインした人だけ」と決めた入口が、実際には誰でも通れる——という差を数える。
 * 差は `tests/architecture/open-doors.test.ts` がコードから測る。
 * 宣言した意図とコードの実測を突き合わせるので、
 * **意図を書き換えて差を消す**ことはできるが、そのときは意図の行が diff に出る。
 *
 * 2026-08-18 の朝の実測は **49 件 / 全 79 件**（管理画面 32 枚 + 誰でも実行できる
 * 変更操作 17 個）だった。画面を一括で守る門が無く、`currentActor()` が
 * 解決できないと見本の身元へ落ちるため、未ログインでも管理画面が開けていた。
 *
 * **同日、入口の門（`src/middleware.ts`）を置いて管理画面 32 枚が外れ、17 件になった。**
 * 残る 17 件は変更を起こす操作で、実際には門の内側にあるが、
 * どの操作がどの画面から呼ばれるかは測れないので、危ない方に倒して数えてある。
 * ここが減るのは各操作が `signedInActor()` を使った日である。
 *
 * この数字は事実の記録であって目標ではない。
 * **上げて緑にすることを禁じる。下げる方向にだけ動かす。**
 */
export const OPEN_DOORS_MAX_UNGUARDED = 17;

/**
 * **誰でも実行できて、しかも取り返しがつかない操作**の上限。
 *
 * 開いている扉のうち、公開・配信・鍵の失効・削除を含むもの。
 * 全体の件数（`OPEN_DOORS_MAX_UNGUARDED`）とは別に数える。
 * 全体が 1 件減っても、減ったのが「下書きを保存する」なら危険は変わらない。
 *
 * 2026-08-18 の実測は **6 件**（記事の公開 / 投稿の予約 / 予約の変更 /
 * 外部連携の鍵の作成と失効 / 生成 AI の API キーの登録と削除 / サイトの作成）。
 *
 * **上げて緑にすることを禁じる。**
 */
export const OPEN_DOORS_MAX_IRREVERSIBLE = 6;

/**
 * **「誰でも」と宣言した行**の上限。
 *
 * 上の 2 つは「意図と実測の差」を数えている。意図を書くのは人なので、
 * ある入口を「誰でも」と宣言すれば、その扉は**差の数から消える**。
 * つまり、上限で詰まったときのいちばん短い道が「宣言を 1 行足す」になる。
 *
 * だから**宣言した件数そのもの**にも上限を置く。
 * 増やすには、この数字を上げる diff が要る。上げた事実は記録に残る。
 *
 * 2026-08-18 の実測は **26 件**（読者向けの公開ページ 20 / 入口の案内 1 /
 * サインイン画面 1 / ログインの往復 1 / 読者からの計測 1 / 成果リンクの転送 1 /
 * 読者からの問い合わせ 1）。**どれも未ログインの読者が使う道である。**
 * 管理側の入口がこの一覧に現れたら、それは宣言で数を消しにいった跡である。
 * 一覧そのものは `docs/product/open-doors.md` の頭に出る。
 *
 * 読者向けのページを新しく足すと、ここは正しく増える。そのときは
 * **足した行が読者の道であることを確かめてから**この数字を上げる。
 * 「増えたから上げる」で通すと、この検査は何も止めなくなる。
 */
export const OPEN_DOORS_MAX_PUBLIC_BY_DECLARATION = 26;
